---
name: pair-agent
version: 0.1.0
description: |
  将 remote AI agent 与你的 browser 配对。一个 command 会生成 setup key，并打印
  另一个 agent 可遵循的连接 instructions。适用于 OpenClaw、Hermes、Codex、Cursor，
  或任何能发 HTTP requests 的 agent。Remote agent 会获得自己的 tab 和 scoped access
  （默认 read+write，可按需 admin）。当用户要求 "pair agent"、"connect agent"、
  "share browser"、"remote browser"、"let another agent use my browser" 或
  "give browser access" 时使用。（gstack）
voice-triggers:
  - "pair agent"
  - "connect agent"
  - "share my browser"
  - "remote browser access"
triggers:
  - pair with agent
  - connect remote agent
  - share my browser
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion

---

{{PREAMBLE}}

# /pair-agent — 与另一个 AI Agent 共享你的 Browser

你正在 Claude Code 里，并且有一个 browser 正在运行。你还打开了另一个 AI agent
（OpenClaw、Hermes、Codex、Cursor，或其他）。你希望那个 agent 能使用你的 browser
浏览 web。此 skill 负责做到这一点。

## How it works（工作方式）

你的 gstack browser 会运行 local HTTP server。此 skill 创建一次性 setup key，打印一段
instructions，然后你将这些 instructions 粘贴到另一个 agent。另一个 agent 用 key 换取
session token，创建自己的 tab，并开始 browsing。每个 agent 都有自己的 tab，不能干扰彼此的 tabs。

Setup key 5 分钟后过期，且只能使用一次。如果它泄露，在任何人能滥用前就已经失效。
Session token 有效期 24 小时。

**Same machine：**如果另一个 agent 在同一台机器上（例如 locally 运行的 OpenClaw），
可以跳过 copy-paste ceremony，直接将 credentials 写入该 agent 的 config directory。

**Remote：**如果另一个 agent 在不同机器上，你需要 ngrok tunnel。此 skill 会告诉你是否需要，
以及如何设置。

{{BROWSE_SETUP}}

## Step 1：检查 prerequisites

```bash
$B status 2>/dev/null
```

如果 browse server 没有运行，启动它：

```bash
$B goto about:blank
```

这确保 pairing 前 server 已启动且 healthy。

## Step 2：询问目标

使用 AskUserQuestion：

> 你想将哪个 agent 与 browser 配对？这会决定 instructions format 和 credentials 写入位置。

选项：
- A) OpenClaw（local 或 remote）
- B) Codex / OpenAI Agents（local）
- C) Cursor（local）
- D) 另一个 Claude Code session（local 或 remote）
- E) 其他目标（generic HTTP instructions；Hermes 用这个）

根据答案设置 `TARGET_HOST`：
- A -> `openclaw`
- B -> `codex`
- C -> `cursor`
- D -> `claude`
- E -> generic（无 host-specific config）

## Step 3：Local 还是 remote？

使用 AskUserQuestion：

> 另一个 agent 是运行在同一台机器上，还是不同机器/server 上？
>
> **Same machine** 会跳过 copy-paste ceremony。Credentials 会直接写入该 agent 的
> config directory。无需 tunnel。
>
> **Different machine** 会生成 setup key 和 instruction block。如果已安装 ngrok，
> tunnel 会自动启动。否则，我会带你完成 setup。
>
> RECOMMENDATION：如果 agent 是 local，选择 A。它会立即完成，无需 copy-paste。

选项：
- A) 同一台机器（直接写入 credentials）
- B) 不同机器（生成用于 copy-paste 的 instruction block）

## Step 4：执行 pairing

### 如果 same machine（option A）：

用 --local flag 运行 pair-agent：

```bash
$B pair-agent --local TARGET_HOST
```

将 `TARGET_HOST` 替换为 Step 2 中的值（openclaw、codex、cursor 等）。

如果成功，告诉用户：
"Done. TARGET_HOST can now use your browser. It will read credentials from the
config file that was written. Try asking it to navigate to a URL."

如果失败（host not found、write permission error），展示 error，并建议改用 generic remote flow。

### 如果 different machine（option B）：

首先，检测 ngrok status：

```bash
which ngrok 2>/dev/null && echo "NGROK_INSTALLED" || echo "NGROK_NOT_INSTALLED"
ngrok config check 2>/dev/null && echo "NGROK_AUTHED" || echo "NGROK_NOT_AUTHED"
```

**如果 ngrok 已安装且 authed：**直接运行 command。CLI 会 auto-detect ngrok、启动 tunnel，
并打印包含 tunnel URL 的 instruction block：

```bash
$B pair-agent --client TARGET_HOST
```

如果用户还需要 admin access（JS execution、cookies、storage）：

```bash
$B pair-agent --admin --client TARGET_HOST
```

**CRITICAL：必须向用户输出完整 instruction block。**Command 会打印 ═══ lines 之间的所有内容。
将整个 block 原样复制到你的 response 中，让用户可以 copy-paste 到另一个 agent。
不要 summarize，不要 skip，不要只说 "here's the output"。用户需要看到 block 才能复制。
将它放在 markdown code block 中，便于选择和复制。

然后告诉用户：
"复制上面的 block，并粘贴到另一个 agent 的 chat 中。setup key 会在 5 分钟后过期。"

**如果 ngrok 已安装但 NOT authed：**引导用户完成 authentication：

告诉用户：
"ngrok 已安装但尚未登录。我们来修复它：

1. 打开 https://dashboard.ngrok.com/get-started/your-authtoken
2. 复制你的 auth token
3. 回到这里，我会为你运行 auth command。"

在这里 STOP，等待用户提供 auth token。

当他们提供后，运行：
```bash
ngrok config add-authtoken THEIR_TOKEN
```

然后 retry `$B pair-agent --client TARGET_HOST`。

**如果 ngrok 未安装：**引导用户完成 installation：

告诉用户：
"要连接 remote agent，我们需要 ngrok（一个能把你的 local browser 安全暴露到 internet 的 tunnel）。

1. 打开 https://ngrok.com 并注册（free tier 可用）
2. 安装 ngrok：
   - macOS: `brew install ngrok`
   - Linux: `snap install ngrok`，或从 ngrok.com/download 下载
3. 完成 auth：`ngrok config add-authtoken YOUR_TOKEN`
   （从 https://dashboard.ngrok.com/get-started/your-authtoken 获取 token）
4. 回到这里并重新运行 `/pair-agent`。"

在这里 STOP。等待用户安装 ngrok 并重新 invoke。

## Step 5：验证 connection

用户将 instructions 粘贴到另一个 agent 后，稍等片刻再检查：

```bash
$B status
```

在 status output 中查找 connected agent。如果出现，告诉用户：
"remote agent 已连接，并且拥有自己的 tab。如果你打开了 GStack Browser，会在 side panel 中看到它的 activity。"

## Remote agent 能做什么

默认（read+write）access 下：
- Navigate to URLs、click elements、fill forms、take screenshots
- Read page content（text、HTML、snapshot）
- Create new tabs（每个 agent 都有自己的）
- 不能 execute arbitrary JavaScript、read cookies 或 access storage

Admin access（--admin flag）下：
- 上述全部能力，加上 JS execution、cookie access、storage access
- 谨慎使用。只给你完全信任的 agents。

## Troubleshooting（故障排查）

**"Tab not owned by your agent"**：Remote agent 试图与它没有创建的 tab 交互。
告诉它先运行 `newtab` 获取自己的 tab。

**"Domain not allowed"**：Token 有 domain restrictions。用更宽的 domain access 或无 domain restrictions 重新 pair。

**"Rate limit exceeded"**：Agent 发送超过 10 requests/second。它应等待 Retry-After header 并放慢。

**"Token expired"**：24-hour session 已过期。再次运行 `/pair-agent` 生成 new setup key。

**Agent can't reach the server**：如果是 remote，检查 ngrok tunnel 是否运行（`$B status`）。
如果是 local，检查 browse server 是否运行。

## Platform-specific notes（平台专属说明）

### OpenClaw / AlphaClaw

OpenClaw agents 使用 `exec` tool，而不是 `Bash`。Instruction block 使用 OpenClaw 原生理解的
`exec curl` syntax。使用 `--local openclaw` 时，credentials 会写入
`~/.openclaw/skills/gstack/browse-remote.json`。


### Codex

Codex agents 可以通过 `codex exec` 执行 shell commands。Instruction block 中的 curl commands
可直接工作。使用 `--local codex` 时，credentials 会写入
`~/.codex/skills/gstack/browse-remote.json`。

### Cursor

Cursor 的 AI 可以运行 terminal commands。Instruction block 可原样工作。
使用 `--local cursor` 时，credentials 会写入
`~/.cursor/skills/gstack/browse-remote.json`.

## Revoking access（撤销访问）

断开特定 agent：

```bash
$B tunnel revoke AGENT_NAME
```

断开所有 agents 并 rotate root token：

```bash
# 这会立即 invalidate 所有 scoped tokens
$B tunnel rotate
```
