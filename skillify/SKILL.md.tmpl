---
name: skillify
version: 1.0.0
description: |
  将最近一次 successful /scrape flow codify 成磁盘上的 permanent browser-skill。
  之后相同 intent 的 /scrape calls 会运行 codified script，约 200ms 返回，
  而不是重新驱动 page。回溯 conversation，合成 script.ts + script.test.ts + fixture，
  在 temp dir 中运行 test，并在 committing 前询问用户。
  当用户要求 "skillify"、"codify"、"save this scrape" 或
  "make this permanent" 时使用。（gstack）
allowed-tools:
  - Bash
  - Read
  - Write
  - AskUserQuestion
triggers:
  - skillify
  - codify this scrape
  - save this scrape
  - make this permanent
---

{{PREAMBLE}}

# /skillify — 将上一次 scrape codify 成 permanent skill

这是 productivity multiplier。`/scrape` 已发现如何拉取 data；`/skillify` 将它写成
deterministic Playwright-via-`browse-client` code，让同一 intent 的下一次 `/scrape`
call 在约 200ms 内运行。

没有此 command，`/scrape` 只是 `$B` 的慢速 wrapper。有了它，每次 successful scrape 都是一次性成本。

## Iron contract — 绝不把 half-broken skill 写到磁盘

Skills 是 user-trust artifacts。`$B skill list` 中的 broken skill 会让 agents 选择错误工具，
并削弱信任。此 skill 先写入 temp dir，在那里运行 auto-generated test；只有在
(a) test pass + (b) explicit user approval 都满足时，才 rename 到 final tier path。
任一失败时，temp dir 都会被完整移除。不存在 "almost shipped" state。

---

## Step 1 — Provenance guard（D1）

回溯 conversation，**最多 10 个 agent turns**，寻找最近一次满足条件的 `/scrape` invocation：

- Bounded（你能识别用户的 intent line，以及 prototype 产生的 trailing JSON）
- 产生了用户之后未 invalidate 的 JSON result
  （例如没有说 "that's wrong"，也没有要求你 retry）

如果找不到，使用精确 message 拒绝：

> "No recent /scrape result found in this conversation. Run /scrape
> <intent> first, then say /skillify."

停止。不要从 chat fragments synthesize。不要从 match-path /scrape result synthesize
（matched skills 已经 codified；没有需要 skillify 的东西）。

如果找到 candidate，但用户当前已经在三轮后讨论无关内容，继续前先问一次：

> "The last successful /scrape was '<intent line>' a few turns back.
> Skillify that one?"

"yes" 允许继续。其他任何回答：用上面的 message 拒绝。

## Step 2 — 提议 name + triggers

从 prototype intent 中提取：

- 短 skill name：lowercase letters/digits/dashes，<=32 chars，
  以字母开头，无连续 dashes。例如：
  `lobsters-frontpage`, `gh-issue-list`, `pypi-package-stats`.
- 3-5 个 trigger phrases，供 agent 在未来 `/scrape` calls 中匹配。混合 canonical phrase
  （"scrape lobsters frontpage"）和 paraphrases（"top posts on lobste.rs"、"lobsters front page"）。
- Host（仅 hostname，例如 `lobste.rs`）。

然后用 **AskUserQuestion** 确认：

```
D<N> — Skill name + tier
Project/branch/task: codifying /scrape "<intent>" as a browser-skill.
ELI10: Pick a short name we'll use to find this skill next time you say
something similar. Pick a tier — global means every project on this
machine sees it, project means just this repo.
Stakes if we pick wrong: bad name buries the skill in $B skill list;
wrong tier means future projects can't find it (or can find it when you
didn't want them to).
Recommendation: A — <proposed-name> at global tier — most scrape skills
generalize across projects.
Note: options differ in kind, not coverage — no completeness score.
A) Keep "<proposed-name>" at global tier — ~/.gstack/browser-skills/<proposed-name>/  (recommended)
B) Keep "<proposed-name>" but at project tier — <project>/.gstack/browser-skills/<proposed-name>/
C) Rename it (free-form — say the new name)
```

**Tier-shadowing check。**展示问题前，运行 `$B skill list`，检查是否已有同名 skill。
如果找到，将以下内容添加到问题中：

> "Note: a <tier> skill named '<name>' already exists. Picking the same
> name at a higher tier (project > global > bundled) shadows it; picking
> the same tier collides and will be refused at write time. Pick a
> different name to coexist."

## Step 3 — Synthesize `script.ts`（D2）

**只使用 final-attempt `$B` calls**：也就是产生用户接受的 JSON 的 calls，加上用户的 intent string。丢弃：

- Failed selector attempts（working one 之前尝试过的四个 selectors）
- 早先 turns 中 unrelated `$B` commands
- 所有 conversation prose、summaries、你自己的 reasoning

Script 从 `./_lib/browse-client` import SDK（step 6 写入的 sibling copy），并 export parser function，
让 `script.test.ts` 可以针对 bundled fixture exercise 它，而无需 spin up daemon。

Mirror bundled reference：`browser-skills/hackernews-frontpage/script.ts`：

```ts
import { browse } from './_lib/browse-client';

export interface Item { /* one row of the JSON output */ }
export interface Output { items: Item[]; count: number; }

const TARGET_URL = '<the URL the prototype used>';

export function parseFromHtml(html: string): Item[] {
  // Pure function: HTML in, parsed Item[] out. No $B calls.
  // Future fixture-replay tests call this directly.
}

if (import.meta.main) { await main(); }

async function main(): Promise<void> {
  await browse.goto(TARGET_URL);
  const html = await browse.html();
  const items = parseFromHtml(html);
  const output: Output = { items, count: items.length };
  process.stdout.write(JSON.stringify(output) + '\n');
}
```

Parser 必须是 pure function。如果 prototype 使用了多个 `$B` calls（例如 goto + click "Next" + html），
将它们保留在 `main()` 中，但把 parsing 提取到 pure helpers。Step 5 中的 fixture-replay tests
只 exercise pure parts。

## Step 4 — Capture fixture（捕获 fixture）

```bash
$B goto "<TARGET_URL>"
$B html > /tmp/skillify-fixture-$$.html
```

Staged dir 内的 fixture filename 是 `fixtures/<host-with-dashes>-<YYYY-MM-DD>.html`，
date 使用今天。例如 `fixtures/lobste-rs-2026-04-27.html`。

读取你写入的 file，将其 contents 存到变量中，并在 step 7 staging 时使用。

## Step 5 — 写入 `script.test.ts`

Mirror `browser-skills/hackernews-frontpage/script.test.ts`。Test 必须至少包含一个 ★★ assertion：
parsed output 有 expected shape 且 key fields 非空，而不是 smoke ★ assertion。
只检查 `parseFromHtml` 不 throw 的 smoke tests 不够。

```ts
import { describe, it, expect } from 'bun:test';
import * as fs from 'fs';
import * as path from 'path';
import { parseFromHtml } from './script';

describe('<name> parser', () => {
  const fixturePath = path.join(import.meta.dir, 'fixtures', '<host>-<date>.html');
  const html = fs.readFileSync(fixturePath, 'utf-8');
  const items = parseFromHtml(html);

  it('returns at least one item from the bundled fixture', () => {
    expect(items.length).toBeGreaterThan(0);
  });

  it('every item has the required shape', () => {
    for (const item of items) {
      expect(typeof item.<keyfield>).toBe('<keytype>');
      // ... assert on every required field
    }
  });
});
```

## Step 6 — Resolve canonical SDK path + read it

Canonical SDK 位于 `<gstack-install>/browse/src/browse-client.ts`。
Bundled-skill loader 会 walk install tree 来找到它；mirror 这个行为。

Resolve gstack install dir。两个可靠 signals（按顺序）：

1. Bundled `hackernews-frontpage` skill：从 `$B skill list`（`bundled` row）查看其 tier path。
   Skill dir 是 `<gstack-install>/browser-skills/hackernews-frontpage/`，
   因此 install dir 是其 `_lib/browse-client.ts` 往上两次 `dirname`。
2. Active gstack skills install：`~/.claude/skills/gstack/`。如果是 symlink，读取 symlink target；
   否则直接使用 path。

Example（以 Bun 运行，不要用 bash，以避免 shell-redirect parsing issues）：

```ts
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';

function resolveSdkPath(): string {
  const candidates = [
    path.join(os.homedir(), '.claude', 'skills', 'gstack', 'browse', 'src', 'browse-client.ts'),
    // Add other install-dir candidates if your environment differs.
  ];
  for (const c of candidates) {
    try {
      const real = fs.realpathSync(c);
      if (fs.existsSync(real)) return real;
    } catch {}
  }
  throw new Error('Could not resolve canonical browse-client.ts');
}

const sdkContents = fs.readFileSync(resolveSdkPath(), 'utf-8');
```

将 SDK contents 读入变量。Staging step 会将其作为 `_lib/browse-client.ts` 写入，
并与 canonical byte-identical。Phase 1 decision #4：每个 skill 都 fully self-contained，
不可能发生 version drift。

## Step 7 — Stage skill（D3 atomic write）

使用 `browse/src/browser-skill-write.ts` 中的 helper。构造 inline TypeScript snippet
（或 shell out 到一个小 Bun one-liner）来调用：

```ts
import { stageSkill } from '<gstack-install>/browse/src/browser-skill-write';

const stagedDir = stageSkill({
  name: '<name>',
  files: new Map([
    ['SKILL.md', skillMd],
    ['script.ts', scriptTs],
    ['script.test.ts', scriptTestTs],
    ['_lib/browse-client.ts', sdkContents],
    ['fixtures/<host>-<date>.html', fixtureHtml],
  ]),
});
console.log(stagedDir);
```

`<name>` 的 SKILL.md content 遵循 Phase 1 frontmatter contract：

```yaml
---
name: <name>
description: <one-line, what data this returns>
host: <hostname>
trusted: false       # agent-authored skills are untrusted by default
source: agent
version: 1.0.0
args: []             # extend if your script accepts --arg key=value
triggers:
  - <phrase 1>
  - <phrase 2>
  - <phrase 3>
---

# <Name> scraper

<2-3 sentences，说明 script 做什么、访问哪个 URL、返回什么
shape of JSON。NO conversation context。NO chat fragments。
这是 durable on-disk artifact；保持简洁。>

## Usage（用法）

\`\`\`
$ $B skill run <name>
{ "items": [...], "count": N }
\`\`\`
```

捕获 `stagedDir`（`stageSkill` 返回的 path）。接下来把它传给 `$B skill test`，
然后传给 `commitSkill` 或 `discardStaged`。

## Step 8 — 针对 staged dir 运行 `$B skill test`

```bash
$B skill test "<name>" --dir "<stagedDir>"
```

如果 `$B skill test` 尚不接受 `--dir`，fallback 到直接针对 staged path 调用 test runner：

```bash
( cd "<stagedDir>" && bun test script.test.ts )
```

如果 test 失败：

1. 阅读 test output。如果 failure 是可修 parser bug，重写 `script.ts` 和 `script.test.ts`
   （仍在 staged dir 内）并 retry：最多两次。每次 retry 前向用户展示 diff。
2. 如果两次 retry 后仍 failing，或 failure 是 environmental issue（SDK import、daemon connection）：

   ```ts
   import { discardStaged } from '<gstack-install>/browse/src/browser-skill-write';
   discardStaged('<stagedDir>');
   ```

   向用户报告 failure，展示 staged `script.ts` 供 reference，然后停止。不会留下 on-disk artifact。

## Step 9 — Approval gate（批准门）

Tests passed。现在 committing 前询问用户：

```
D<N> — Commit skill "<name>" at <resolved-tier-path>?
Project/branch/task: codified /scrape "<intent>" — tests pass against fixture.
ELI10: Script 已经在我们捕获的 snapshot 上 clean run。选择 yes 会把 staged folder
移入 ~/.gstack/browser-skills/，下次 /scrape 会在那里找到它。选择 no 会移除
staged folder，且不会有任何东西落盘。
Stakes if we pick wrong: yes 会 commit 一个 artifact；如果之后后悔，需要手动 rm
（$B skill rm <name> --global）。No 会丢掉约 30s synthesis work。
Recommendation: A — tests passed, the script is self-contained, this is
the productivity payoff for the prototype.
Note: options differ in kind, not coverage — no completeness score.
A) Commit it (recommended)
B) Look at the script first (I'll print SKILL.md + script.ts and re-ask)
C) Discard — don't commit
```

如果用户选择 B，打印 staged `SKILL.md` 和 `script.ts`（不要打印 fixture 或 _lib/），
然后重新询问同一个 A/B/C question（这次不带 B：他们已经看过了）。

## Step 10 — Commit（atomic）或 discard

如果用户 approved：

```ts
import { commitSkill } from '<gstack-install>/browse/src/browser-skill-write';
const dest = commitSkill({
  name: '<name>',
  tier: '<global|project>',  // from step 2 answer
  stagedDir: '<stagedDir>',
});
console.log(`Committed: ${dest}`);
```

如果 `commitSkill` throws "already exists"（用户在 step 2 忽略的 tier-shadowing collision），
报告并询问是否要：

- 选择 different name（回到 step 2）
- `$B skill rm <name>` 后 retry
- Discard

如果用户在 step 9 rejected：

```ts
import { discardStaged } from '<gstack-install>/browse/src/browser-skill-write';
discardStaged('<stagedDir>');
```

Report："Discarded. No skill was written to disk."

## Step 11 — Confirm + verify

Successful commit 后，运行一次 verification：

```bash
$B skill list | grep <name>
$B skill run <name>    # should match the JSON the prototype produced
```

如果 post-commit run 不匹配 prototype output，说明 synthesis 中有 drift。向用户明确说明：
他们可能想要 `$B skill rm <name>` 并 retry。不要 silent rollback；用户应该看到 discrepancy。

用一行结束 skill："Skill '<name>' committed at <tier>. Future
/scrape calls matching '<canonical-trigger>' will run in ~200ms."

---

## Limits（be honest）

- **需要 Bun runtime。**Codified skill 作为 Bun process 运行（`bun run script.ts`）。
  Phase 1 design carry-over（Codex finding #7）。真正 fix 会在 Phase 4 落地
  （self-contained binary 或 Node fallback）。目前：skill 可在任何安装了 gstack 的机器上工作，
  也就意味着它有 Bun。
- **Fixture-replay tests 是 point-in-time。**当 target site 轮换 HTML 时，fixture 会 stale，
  test 会 against outdated snapshot pass。Phase 4 会添加 fixture-staleness detection。
- **Synthesis 是 best-effort。**你是从自己的 conversation memory 写 script。如果 prototype 很复杂
  （multi-page、JS hydration、lazy load），codified script 可能需要 hand-edit 才可靠。
  Post-commit verify step 会捕捉明显 drift。
- **Single-target only。**每个 skill 一个 `$B goto` URL。Multi-page crawls 不在 scope：
  为每个 target 写 separate skill，或在 URL pattern 规则时通过 `args:` parameterize。

## 此 skill 不做什么

- Codify match-path /scrape results（matched skills 已经 codified）
- Codify mutating flows（那是 /automate 的工作：Phase 2 P0）
- Run skills（那是 `$B skill run`：codified skills 通过 /scrape 的 match path 或直接运行）
- Edit existing skills（surface 是 $EDITOR + skill dir；`$B skill show <name>` 可找到 path）
- Tombstone 或 remove（$B skill rm）

{{LEARNINGS_LOG}}
