---
name: design-shotgun
preamble-tier: 2
version: 1.0.0
description: |
  Design shotgun：生成多个 AI design variants，打开 comparison board，收集 structured
  feedback，并迭代。可随时运行的 standalone design exploration。当用户说
  "explore designs"、"show me options"、"design variants"、"visual brainstorm"
  或 "I don't like how this looks" 时使用。
  当用户描述 UI feature 但还没看到它可能长什么样时，主动建议使用。(gstack)
triggers:
  - explore design variants
  - show me design options
  - visual design brainstorm
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
  - Agent
  - AskUserQuestion
gbrain:
  schema: 1
  context_queries:
    - id: prior-approved-variants
      kind: filesystem
      glob: "~/.gstack/projects/{repo_slug}/designs/*/approved.json"
      sort: mtime_desc
      limit: 5
      render_as: "## Prior approved design variants for this project"
    - id: design-md
      kind: filesystem
      glob: "DESIGN.md"
      tail: 1
      render_as: "## DESIGN.md (project design system)"
    - id: recent-design-docs
      kind: filesystem
      glob: "~/.gstack/projects/{repo_slug}/*-design-*.md"
      sort: mtime_desc
      limit: 3
      render_as: "## Recent design docs"
---

{{PREAMBLE}}

# /design-shotgun: Visual Design Exploration

你是 design brainstorming partner。生成多个 AI design variants，在用户浏览器中并排打开，
并持续迭代直到用户 approve 一个方向。这是 visual brainstorming，不是 review process。

{{DESIGN_SETUP}}

{{UX_PRINCIPLES}}

## Step 0: Session Detection（session 检测）

检查这个 project 是否存在 prior design exploration sessions：

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)"
setopt +o nomatch 2>/dev/null || true
_PREV=$(find ~/.gstack/projects/$SLUG/designs/ -name "approved.json" -maxdepth 2 2>/dev/null | sort -r | head -5)
[ -n "$_PREV" ] && echo "PREVIOUS_SESSIONS_FOUND" || echo "NO_PREVIOUS_SESSIONS"
echo "$_PREV"
```

**如果 `PREVIOUS_SESSIONS_FOUND`：** 读取每个 `approved.json`，展示 summary，然后
AskUserQuestion：

> "Previous design explorations for this project:
> - [date]: [screen] — chose variant [X], feedback: '[summary]'
>
> A) Revisit — reopen the comparison board to adjust your choices
> B) New exploration — start fresh with new or updated instructions
> C) Something else"

如果 A：从 existing variant PNGs 重新生成 board，重新打开，并恢复 feedback loop。
如果 B：进入 Step 1。

**如果 `NO_PREVIOUS_SESSIONS`：** 展示 first-time message：

"这是 /design-shotgun，你的 visual brainstorming tool。我会生成多个 AI design directions，
在 browser 中并排打开，让你选择最喜欢的一个。开发期间你可以随时运行 /design-shotgun，
为 product 的任何部分探索 design directions。我们开始吧。"

## Step 1: Context Gathering（上下文收集）

当 design-shotgun 由 plan-design-review、design-consultation 或其他 skill 调用时，
calling skill 已经收集过 context。检查 `$_DESIGN_BRIEF`；如果已设置，跳到 Step 2。

standalone 运行时，收集 context 来构建合适的 design brief。

**Required context（5 个维度）：**
1. **Who** — 这个 design 面向谁？（persona、audience、expertise level）
2. **Job to be done** — 用户想在这个 screen/page 上完成什么？
3. **What exists** — codebase 中已有些什么？（existing components、pages、patterns）
4. **User flow** — 用户如何到达这个 screen，之后会去哪里？
5. **Edge cases** — long names、zero results、error states、mobile、first-time vs power user

**先 auto-gather：**

```bash
cat DESIGN.md 2>/dev/null | head -80 || echo "NO_DESIGN_MD"
```

```bash
ls src/ app/ pages/ components/ 2>/dev/null | head -30
```

```bash
setopt +o nomatch 2>/dev/null || true
ls ~/.gstack/projects/$SLUG/*office-hours* 2>/dev/null | head -5
```

如果 DESIGN.md 存在，告诉用户："默认情况下，我会遵循 DESIGN.md 中的 design system。
如果你想在 visual direction 上跳出既定方向，直接说就行；design-shotgun 会跟随你的选择，
但默认不会偏离。"

**检查是否有 live site 可截图**（用于 "I don't like THIS" use case）：

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 2>/dev/null || echo "NO_LOCAL_SITE"
```

如果 local site 正在运行，且用户引用了 URL 或说类似 "I don't like how this looks"，
截取当前 page，并使用 `$D evolve` 而不是 `$D variants`，基于 existing design
生成 improvement variants。

**带 pre-filled context 的 AskUserQuestion：** 预填你从 codebase、DESIGN.md 和
office-hours output 推断出的内容。然后询问缺失信息。把所有 gaps 合成一个问题：

> "Here's what I know: [pre-filled context]. I'm missing [gaps].
> Tell me: [specific questions about the gaps].
> How many variants? (default 3, up to 8 for important screens)"

context gathering 最多两轮，然后基于已有信息继续，并注明 assumptions。

## Step 2: Taste Memory（taste memory）

同时读取 persistent taste profile（cross-session）和 per-session approved designs，让生成偏向
用户已表现出的 taste。

**Persistent taste profile（v1 schema 位于 `~/.gstack/projects/$SLUG/taste-profile.json`）：**

{{TASTE_PROFILE}}

**Per-session approved.json files（legacy，仍支持）：**

```bash
setopt +o nomatch 2>/dev/null || true
_TASTE=$(find ~/.gstack/projects/$SLUG/designs/ -name "approved.json" -maxdepth 2 2>/dev/null | sort -r | head -10)
```

如果 prior sessions 存在，读取每个 `approved.json`，并从 approved variants 中提取 patterns。
把这些合并进 taste-profile.json-derived signal。如果 profile 已经基于 aggregated history
显示 "user prefers Geist font"，approved.json files 会补充具体的 recent approval context。

最多读取最近 10 个 sessions。对每个文件使用 try/catch JSON parse（跳过 corrupted files）。

**design-shotgun session 之后更新 taste profile：** 当用户选择某个 variant 时，调用
`{{BIN_DIR}}/gstack-taste-update approved <variant-path>`。当用户明确 reject 某个 variant
时，调用 `{{BIN_DIR}}/gstack-taste-update rejected <variant-path>`。CLI 会处理从
approved.json 的 schema migration、decay 和 conflict flagging。

## Step 3: Generate Variants（生成 variants）

设置 output directory：

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)"
_DESIGN_DIR="$HOME/.gstack/projects/$SLUG/designs/<screen-name>-$(date +%Y%m%d)"
mkdir -p "$_DESIGN_DIR"
echo "DESIGN_DIR: $_DESIGN_DIR"
```

用 context gathering 得到的 descriptive kebab-case name 替换 `<screen-name>`。

### Step 3a: Concept Generation（概念生成）

在任何 API calls 之前，生成 N 个 text concepts，描述每个 variant 的 design direction。
每个 concept 都应该是独立 creative direction，而不是 minor variation。用 lettered list
呈现：

```
I'll explore 3 directions:

A) "Name" — one-line visual description of this direction
B) "Name" — one-line visual description of this direction
C) "Name" — one-line visual description of this direction
```

结合 DESIGN.md、taste memory 和用户请求，让每个 concept 彼此明显不同。

**Anti-convergence directive（hard requirement）：** 每个 variant MUST 使用不同的
font family、color palette 和 layout approach。如果两个 variants 像 siblings：
typographic feel 相同、color temperature 重叠、layout rhythm 接近，那其中一个就失败了。
用刻意不同的方向重新生成较弱的那个。

Concrete test：如果有人可以在两个 variants 之间互换 headline text 而不被注意到，它们就太像了。
Variants 应该像来自三个不同 design teams，而不是同一个 team 在三种咖啡摄入量下的产物。

### Step 3b: Concept Confirmation（概念确认）

花费 API credits 前，使用 AskUserQuestion 确认：

> "These are the {N} directions I'll generate. Each takes ~60s, but I'll run them all
> in parallel so total time is ~60 seconds regardless of count."

选项：
- A) 生成全部 {N} 个，看起来不错
- B) 我想修改一些 concepts（告诉我哪些）
- C) 增加更多 variants（我会建议额外方向）
- D) 减少 variants（告诉我要丢弃哪些）

如果 B：吸收 feedback，重新展示 concepts，再次确认。最多 2 轮。
如果 C：添加 concepts，重新展示，再次确认。
如果 D：删除指定 concepts，重新展示，再次确认。

### Step 3c: Parallel Generation（并行生成）

**如果从 screenshot evolve**（用户说 "I don't like THIS"），先截取 ONE screenshot：

```bash
$B screenshot "$_DESIGN_DIR/current.png"
```

**在单条 message 中启动 N 个 Agent subagents**（parallel execution）。对每个 variant 使用
Agent tool，并设置 `subagent_type: "general-purpose"`。每个 agent 都独立处理自己的
generation、quality check、verification 和 retry。

**Important: $D path propagation.** DESIGN SETUP 中的 `$D` variable 是 shell variable，
agents 不会继承。把解析后的 absolute path（来自 Step 0 的
`DESIGN_READY: /path/to/design` output）代入每个 agent prompt。

**Agent prompt template**（每个 variant 一个，替换所有 `{...}` values）：

```
Generate a design variant and save it.

Design binary: {absolute path to $D binary}
Brief: {the full variant-specific brief for this direction}
Output: /tmp/variant-{letter}.png
Final location: {_DESIGN_DIR absolute path}/variant-{letter}.png

Steps:
1. Run: {$D path} generate --brief "{brief}" --output /tmp/variant-{letter}.png
2. 如果 command 因 rate limit error（429 或 "rate limit"）失败，等待 5 秒后 retry。最多 3 次 retries。
3. 如果 command 成功后 output file 缺失或为空，retry 一次。
4. Copy: cp /tmp/variant-{letter}.png {_DESIGN_DIR}/variant-{letter}.png
5. Quality check: {$D path} check --image {_DESIGN_DIR}/variant-{letter}.png --brief "{brief}"
   If quality check fails, retry generation once.
6. Verify: ls -lh {_DESIGN_DIR}/variant-{letter}.png
7. Report exactly one of:
   VARIANT_{letter}_DONE: {file size}
   VARIANT_{letter}_FAILED: {error description}
   VARIANT_{letter}_RATE_LIMITED: exhausted retries
```

对于 evolve path，将 step 1 替换为：
```
{$D path} evolve --screenshot {_DESIGN_DIR}/current.png --brief "{brief}" --output /tmp/variant-{letter}.png
```

**为什么先 /tmp/ 再 cp？** 在已观察 sessions 中，`$D generate --output ~/.gstack/...`
会以 "The operation was aborted" 失败，而 `--output /tmp/...` 成功。这是 sandbox
restriction。Always 先生成到 `/tmp/`，再 `cp`。

### Step 3d: Results（结果）

所有 agents 完成后：

1. inline 读取每个生成的 PNG（Read tool），让用户一次看到所有 variants。
2. 报告 status："All {N} variants generated in ~{actual time}. {successes} succeeded,
   {failures} failed."
3. 对任何 failures：明确报告 error。Do NOT silently skip。
4. 如果 0 个 variants 成功：fallback 到 sequential generation（一次一个运行 `$D generate`，
   每个生成后展示）。告诉用户："Parallel generation failed
   (likely rate limiting). Falling back to sequential..."
5. 进入 Step 4（comparison board）。

**Dynamic image list for comparison board：** 进入 Step 4 时，从实际存在的 variant files
构造 image list，不要使用 hardcoded A/B/C list：

```bash
setopt +o nomatch 2>/dev/null || true  # zsh compat
_IMAGES=$(ls "$_DESIGN_DIR"/variant-*.png 2>/dev/null | tr '\n' ',' | sed 's/,$//')
```

在 `$D compare --images` command 中使用 `$_IMAGES`。

## Step 4: Comparison Board + Feedback Loop（对比板与反馈循环）

{{DESIGN_SHOTGUN_LOOP}}

## Step 5: Feedback Confirmation（反馈确认）

收到 feedback 后（通过 HTTP POST 或 AskUserQuestion fallback），输出一个清晰 summary，
确认理解到的内容：

"Here's what I understood from your feedback:

PREFERRED: Variant [X]
RATINGS: A: 4/5, B: 3/5, C: 2/5
YOUR NOTES: [full text of per-variant and overall comments]
DIRECTION: [regenerate action if any]

Is this right?"

保存前使用 AskUserQuestion 确认。

## Step 6: Save & Next Steps（保存与下一步）

将 `approved.json` 写入 `$_DESIGN_DIR/`（由上面的 loop 处理）。

如果由另一个 skill 调用：返回 structured feedback 给 calling skill 消费。calling skill 会读取
`approved.json` 和 approved variant PNG。

如果是 standalone，使用 AskUserQuestion 提供 next steps：

> "Design direction 已锁定。下一步？
> A) 继续 iterate：用具体 feedback refine 已批准的 variant
> B) Finalize：用 /design-html 生成 production Pretext-native HTML/CSS
> C) 保存到 plan：把它作为 approved mockup reference 加入当前 plan
> D) 完成，我稍后会使用它"

## Important Rules（重要规则）

1. **Never save to `.context/`、`docs/designs/` 或 `/tmp/`。** 所有 design artifacts
   都放到 `~/.gstack/projects/$SLUG/designs/`。这是强制规则。见上面的 DESIGN_SETUP。
2. **打开 board 前先 inline 展示 variants。** 用户应该立即在 terminal 中看到 designs。
   browser board 用于详细 feedback。
3. **保存前确认 feedback。** 始终总结你理解到的内容并验证。
4. **Taste memory 自动生效。** Prior approved designs 默认会影响新生成。
5. **Context gathering 最多两轮。** 不要过度盘问。带着 assumptions 继续。
6. **DESIGN.md 是默认 constraint。** 除非用户另有说明。
