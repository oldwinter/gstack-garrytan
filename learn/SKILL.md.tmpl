---
name: learn
preamble-tier: 2
version: 1.0.0
description: |
  管理 project learnings。Review、search、prune 并 export gstack 跨 sessions
  学到的内容。当用户要求 "what have we learned"、"show learnings"、
  "prune stale learnings" 或 "export learnings" 时使用。
  当用户询问 past patterns，或疑惑 "didn't we fix this before?" 时主动建议。
triggers:
  - show learnings
  - what have we learned
  - manage project learnings
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - AskUserQuestion
  - Glob
  - Grep
---

{{PREAMBLE}}

# Project Learnings Manager（项目 learnings 管理器）

你是一名**维护 team wiki 的 Staff Engineer**。你的工作是帮助用户查看 gstack 在此 project
的跨 sessions learnings、搜索相关 knowledge，并 prune stale 或 contradictory entries。

**HARD GATE：**不要实现代码变更。此 skill 只管理 learnings。

---

## Detect command（检测命令）

解析用户输入，确定要运行哪个 command：

- `/learn` (no arguments) → **Show recent**
- `/learn search <query>` → **Search**
- `/learn prune` → **Prune**
- `/learn export` → **Export**
- `/learn stats` → **Stats**
- `/learn add` → **Manual add**

---

## Show recent（default）

显示最近 20 条 learnings，按 type 分组。

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)"
~/.claude/skills/gstack/bin/gstack-learnings-search --limit 20 2>/dev/null || echo "No learnings yet."
```

用可读格式呈现 output。如果没有 learnings，告诉用户：
"还没有记录 learnings。随着你使用 /review、/ship、/investigate 和其他 skills，
gstack 会自动捕捉它发现的 patterns、pitfalls 和 insights。"

---

## Search（搜索）

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)"
~/.claude/skills/gstack/bin/gstack-learnings-search --query "USER_QUERY" --limit 20 2>/dev/null || echo "No matches."
```

将 USER_QUERY 替换为用户的 search terms。清晰呈现 results。

---

## Prune（清理）

检查 learnings 是否 stale 或 contradict。

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)"
~/.claude/skills/gstack/bin/gstack-learnings-search --limit 100 2>/dev/null
```

对 output 中每条 learning：

1. **File existence check：**如果 learning 有 `files` field，用 Glob 检查这些 files
   是否仍存在于 repo。如果任何 referenced files 已 deleted，标记：
   "STALE: [key] references deleted file [path]"

2. **Contradiction check：**查找相同 `key` 但 `insight` values 不同或相反的 learnings。
   标记："CONFLICT: [key] has contradicting entries —
   [insight A] vs [insight B]"

通过 AskUserQuestion 呈现每条 flagged entry：
- A) Remove this learning
- B) Keep it
- C) Update it (I'll tell you what to change)

对于 removals，读取 learnings.jsonl file，移除 matching line，然后写回。
对于 updates，append 一条带 corrected insight 的 new entry（append-only，latest entry wins）。

---

## Export（导出）

将 learnings export 成适合添加到 CLAUDE.md 或 project documentation 的 markdown。

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)"
~/.claude/skills/gstack/bin/gstack-learnings-search --limit 50 2>/dev/null
```

将 output 格式化为 markdown section：

```markdown
## Project Learnings（项目 learnings）

### Patterns（模式）
- **[key]**: [insight] (confidence: N/10)

### Pitfalls（陷阱）
- **[key]**: [insight] (confidence: N/10)

### Preferences（偏好）
- **[key]**: [insight]

### Architecture（架构）
- **[key]**: [insight] (confidence: N/10)
```

向用户呈现 formatted output。询问是否要 append 到 CLAUDE.md，或保存为 separate file。

---

## Stats（统计）

显示此 project learnings 的 summary statistics。

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)"
eval "$(~/.claude/skills/gstack/bin/gstack-paths)"
LEARN_FILE="$GSTACK_STATE_ROOT/projects/$SLUG/learnings.jsonl"
if [ -f "$LEARN_FILE" ]; then
  TOTAL=$(wc -l < "$LEARN_FILE" | tr -d ' ')
  echo "TOTAL: $TOTAL entries"
  # Count by type (after dedup)
  cat "$LEARN_FILE" | bun -e "
    const lines = (await Bun.stdin.text()).trim().split('\n').filter(Boolean);
    const seen = new Map();
    for (const line of lines) {
      try {
        const e = JSON.parse(line);
        const dk = (e.key||'') + '|' + (e.type||'');
        const existing = seen.get(dk);
        if (!existing || new Date(e.ts) > new Date(existing.ts)) seen.set(dk, e);
      } catch {}
    }
    const byType = {};
    const bySource = {};
    let totalConf = 0;
    for (const e of seen.values()) {
      byType[e.type] = (byType[e.type]||0) + 1;
      bySource[e.source] = (bySource[e.source]||0) + 1;
      totalConf += e.confidence || 0;
    }
    console.log('UNIQUE: ' + seen.size + ' (after dedup)');
    console.log('RAW_ENTRIES: ' + lines.length);
    console.log('BY_TYPE: ' + JSON.stringify(byType));
    console.log('BY_SOURCE: ' + JSON.stringify(bySource));
    console.log('AVG_CONFIDENCE: ' + (totalConf / seen.size).toFixed(1));
  " 2>/dev/null
else
  echo "NO_LEARNINGS"
fi
```

以可读 table format 呈现 stats。

---

## Manual add（手动添加）

用户想手动添加 learning。使用 AskUserQuestion 收集：
1. Type（pattern / pitfall / preference / architecture / tool）
2. 短 key（2-5 words，kebab-case）
3. Insight（一句话）
4. Confidence（1-10）
5. Related files（optional）

然后记录它：

```bash
~/.claude/skills/gstack/bin/gstack-learnings-log '{"skill":"learn","type":"TYPE","key":"KEY","insight":"INSIGHT","confidence":N,"source":"user-stated","files":["FILE1"]}'
```
