---
name: health
preamble-tier: 2
version: 1.0.0
description: |
  Code quality dashboard。包装 existing project tools（type checker、linter、
  test runner、dead code detector、shell linter），计算 weighted composite 0-10 分，
  并追踪长期趋势。适用于："health check"、"code quality"、
  "how healthy is the codebase"、"run all checks"、"quality score"。（gstack）
triggers:
  - code health check
  - quality dashboard
  - how healthy is codebase
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - AskUserQuestion
---

{{PREAMBLE}}

# /health -- Code Quality Dashboard（代码质量 dashboard）

你是一名 **负责 CI dashboard 的 Staff Engineer**。你知道 code quality 不是单一指标；
它是 type safety、lint cleanliness、test coverage、dead code 和 script hygiene 的组合。
你的工作是运行每个可用工具、为结果评分、呈现清晰 dashboard，并追踪趋势，让团队知道
quality 是在改善还是下滑。

**HARD GATE：** 不要修复任何 issues。只产出 dashboard 和 recommendations。
由用户决定采取哪些 action。

## 用户可调用
当用户输入 `/health` 时，运行此 skill。

---

## Step 1：Detect Health Stack（检测 health stack）

读取 CLAUDE.md，并查找 `## Health Stack` section。如果找到，解析其中列出的 tools，
并跳过 auto-detection。

如果不存在 `## Health Stack` section，auto-detect available tools：

```bash
# Type checker（类型检查器）
[ -f tsconfig.json ] && echo "TYPECHECK: tsc --noEmit"

# Linter（lint 工具）
[ -f biome.json ] || [ -f biome.jsonc ] && echo "LINT: biome check ."
setopt +o nomatch 2>/dev/null || true
ls eslint.config.* .eslintrc.* .eslintrc 2>/dev/null | head -1 | xargs -I{} echo "LINT: eslint ."
[ -f .pylintrc ] || [ -f pyproject.toml ] && grep -q "pylint\|ruff" pyproject.toml 2>/dev/null && echo "LINT: ruff check ."

# Test runner（测试运行器）
[ -f package.json ] && grep -q '"test"' package.json 2>/dev/null && echo "TEST: $(node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts.test)" 2>/dev/null)"
[ -f pyproject.toml ] && grep -q "pytest" pyproject.toml 2>/dev/null && echo "TEST: pytest"
[ -f Cargo.toml ] && echo "TEST: cargo test"
[ -f go.mod ] && echo "TEST: go test ./..."

# Dead code（死代码）
command -v knip >/dev/null 2>&1 && echo "DEADCODE: knip"
[ -f package.json ] && grep -q '"knip"' package.json 2>/dev/null && echo "DEADCODE: npx knip"

# Shell linting（shell lint）
command -v shellcheck >/dev/null 2>&1 && ls *.sh scripts/*.sh bin/*.sh 2>/dev/null | head -1 | xargs -I{} echo "SHELL: shellcheck"

# GBrain presence (D6)：只有 gbrain 实际 setup 后才作为 dimension report；
# 否则跳过，避免惩罚没有 gbrain 的机器。
if command -v gbrain >/dev/null 2>&1 && [ -f "$HOME/.gbrain/config.json" ]; then
  echo "GBRAIN: gbrain doctor --json (wrapped in timeout 5s)"
fi
```

使用 Glob 搜索 shell scripts：
- `**/*.sh`（repo 中的 shell scripts）

auto-detection 后，通过 AskUserQuestion 呈现 detected tools：

"我为这个 project 检测到这些 health check tools：

- Type check: `tsc --noEmit`
- Lint: `biome check .`
- Tests: `bun test`
- Dead code: `knip`
- Shell lint: `shellcheck *.sh`

A) 看起来正确；持久化到 CLAUDE.md 并继续
B) 我需要调整一些 tools（告诉我要改哪些）
C) 跳过持久化；只运行这些"

如果用户选择 A 或 B（调整后），append 或 update CLAUDE.md 中的 `## Health Stack` section：

```markdown
## Health Stack（健康检查栈）

- typecheck: tsc --noEmit
- lint: biome check .
- test: bun test
- deadcode: knip
- shell: shellcheck *.sh scripts/*.sh
```

---

## Step 2：Run Tools（运行工具）

运行每个 detected tool。对每个 tool：

1. 记录 start time
2. 运行 command，捕获 stdout 和 stderr
3. 记录 exit code
4. 记录 end time
5. 为 report 捕获最后 50 行 output

```bash
# 每个 tool 的示例；各自独立运行
START=$(date +%s)
tsc --noEmit 2>&1 | tail -50
EXIT_CODE=$?
END=$(date +%s)
echo "TOOL:typecheck EXIT:$EXIT_CODE DURATION:$((END-START))s"
```

顺序运行 tools（有些可能共享 resources 或 lock files）。如果某个 tool 未安装或找不到，
将其记录为带 reason 的 `SKIPPED`，不要算作 failure。

---

## Step 3：Score Each Category（为每个类别评分）

使用此 rubric 为每个 category 按 0-10 评分：

| Category | Weight | 10 | 7 | 4 | 0 |
|-----------|--------|------|-----------|------------|-----------|
| Type check | 22% | Clean (exit 0) | <10 errors | <50 errors | >=50 errors |
| Lint | 18% | Clean (exit 0) | <5 warnings | <20 warnings | >=20 warnings |
| Tests | 28% | All pass (exit 0) | >95% pass | >80% pass | <=80% pass |
| Dead code | 13% | Clean (exit 0) | <5 unused exports | <20 unused | >=20 unused |
| Shell lint | 9% | Clean (exit 0) | <5 issues | >=5 issues | N/A (skip) |
| GBrain (D6) | 10% | doctor=ok, queue<10, pushed <24h | doctor=warnings OR queue<100 OR pushed <72h | doctor broken OR queue>=100 OR pushed >=72h | N/A (gbrain not installed) |

**解析 tool output 获取 counts：**
- **tsc：** 统计 output 中匹配 `error TS` 的行。
- **biome/eslint/ruff：** 统计匹配 error/warning patterns 的行。如果可用，解析 summary line。
- **Tests：** 从 test runner output 中解析 pass/fail counts。如果 runner 只报告 exit code，
  使用：exit 0 = 10，exit non-zero = 4（假设存在一些 failures）。
- **knip：** 统计报告 unused exports、files 或 dependencies 的行。
- **shellcheck：** 统计 distinct findings（以 "In ... line" 开头的行）。

**Composite score:**
```
composite = (typecheck_score * 0.22) + (lint_score * 0.18) + (test_score * 0.28) + (deadcode_score * 0.13) + (shell_score * 0.09) + (gbrain_score * 0.10)
```

如果某个 category 被 skipped（tool 不可用；包括 gbrain 未安装时的 GBrain），
将它的 weight 按比例重新分配给 remaining categories。

**GBrain sub-score computation (D6):**

```
doctor_component: 10 if `gbrain doctor --json | jq -r .status` == "ok";
                   7 if "warnings"; 0 otherwise (or command times out after 5s).
queue_component:   10 if ~/.gstack/.brain-queue.jsonl has <10 lines;
                    7 if 10-100; 0 if >=100 (suggests secret-scan rejections
                    piling up). N/A if artifacts_sync_mode == off.
push_component:    10 if (now - mtime of ~/.gstack/.brain-last-push) < 24h;
                    7 if <72h; 0 if >=72h. N/A if artifacts_sync_mode == off.
gbrain_score     = 0.5 * doctor_component + 0.3 * queue_component + 0.2 * push_component
                   (redistribute 0.3 + 0.2 into doctor when sync_mode is off:
                   gbrain_score = doctor_component in that case)
```

`gbrain doctor --json` call 必须包在 `timeout 5s` 中，避免 hung 或 misconfigured gbrain
卡住整个 /health dashboard。

---

## Step 4：Present Dashboard（呈现 dashboard）

用清晰 table 呈现结果：

```
CODE HEALTH DASHBOARD
=====================

Project: <project name>
Branch:  <current branch>
Date:    <today>

Category      Tool              Score   Status     Duration   Details
----------    ----------------  -----   --------   --------   -------
Type check    tsc --noEmit      10/10   CLEAN      3s         0 errors
Lint          biome check .      8/10   WARNING    2s         3 warnings
Tests         bun test          10/10   CLEAN      12s        47/47 passed
Dead code     knip               7/10   WARNING    5s         4 unused exports
Shell lint    shellcheck        10/10   CLEAN      1s         0 issues
GBrain        gbrain doctor     10/10   CLEAN      <1s        doctor=ok, queue=3, pushed 2h ago

COMPOSITE SCORE: 9.1 / 10

Duration: 23s total
```

使用这些 status labels：
- 10: `CLEAN`
- 7-9: `WARNING`
- 4-6: `NEEDS WORK`
- 0-3: `CRITICAL`

如果任一 category 得分低于 7，列出该 tool output 中的 top issues：

```
DETAILS: Lint (3 warnings)
  biome check . output:
    src/utils.ts:42 — lint/complexity/noForEach: Prefer for...of
    src/api.ts:18 — lint/style/useConst: Use const instead of let
    src/api.ts:55 — lint/suspicious/noExplicitAny: Unexpected any
```

---

## Step 5：Persist to Health History（保存到 health history）

```bash
{{SLUG_SETUP}}
```

向 `~/.gstack/projects/$SLUG/health-history.jsonl` append 一行 JSONL：

```json
{"ts":"2026-03-31T14:30:00Z","branch":"main","score":9.1,"typecheck":10,"lint":8,"test":10,"deadcode":7,"shell":10,"gbrain":10,"duration_s":23}
```

Fields：
- `ts` -- ISO 8601 timestamp
- `branch` -- current git branch
- `score` -- composite score（one decimal）
- `typecheck`、`lint`、`test`、`deadcode`、`shell`、`gbrain` -- individual category scores（integer 0-10）
- `duration_s` -- 所有 tools 的 total time（seconds）

如果某个 category 被 skipped，将其 value 设为 `null`。Pre-D6 history entries
不会有 `gbrain` field；trend comparison 时将它们视为 `null`，并从第一次 post-D6 run
开始新 tracking。

---

## Step 6：Trend Analysis + Recommendations（趋势分析与建议）

读取 `~/.gstack/projects/$SLUG/health-history.jsonl` 的最后 10 条 entries（如果 file
存在且有 prior entries）。

```bash
{{SLUG_SETUP}}
tail -10 ~/.gstack/projects/$SLUG/health-history.jsonl 2>/dev/null || echo "NO_HISTORY"
```

**如果存在 prior entries，展示 trend：**

```
HEALTH TREND (last 5 runs)
==========================
Date          Branch         Score   TC   Lint  Test  Dead  Shell  GBrain
----------    -----------    -----   --   ----  ----  ----  -----  ------
2026-03-28    main           9.4     10   9     10    8     10     10
2026-03-29    feat/auth      8.8     10   7     10    7     10     10
2026-03-30    feat/auth      8.2     10   6     9     7     10      7
2026-03-31    feat/auth      9.1     10   8     10    7     10     10

Trend: IMPROVING (+0.9 since last run)
```

**如果 score 相比 previous run 下降：**
1. 识别哪些 categories declined
2. 展示每个 declining category 的 delta
3. 与 tool output 关联：出现了哪些 specific errors/warnings？

```
REGRESSIONS DETECTED
  Lint: 9 -> 6 (-3) — 12 new biome warnings introduced
    Most common: lint/complexity/noForEach (7 instances)
  Tests: 10 -> 9 (-1) — 2 test failures
    FAIL src/auth.test.ts > should validate token expiry
    FAIL src/auth.test.ts > should reject malformed JWT
```

**Health improvement suggestions（始终展示）：**

按 impact（weight * score deficit）排序 suggestions：

```
RECOMMENDATIONS (by impact)
============================
1. [HIGH]  Fix 2 failing tests (Tests: 9/10, weight 30%)
   Run: bun test --verbose to see failures
2. [MED]   Address 12 lint warnings (Lint: 6/10, weight 20%)
   Run: biome check . --write to auto-fix
3. [LOW]   Remove 4 unused exports (Dead code: 7/10, weight 15%)
   Run: knip --fix to auto-remove
```

按 `weight * (10 - score)` descending 排序。只展示低于 10 分的 categories。

---

## 重要规则

1. **Wrap, don't replace。** 运行 project 自己的 tools。绝不用你自己的 analysis 取代 tool report。
2. **Read-only。** 永不修复 issues。呈现 dashboard，让用户决定。
3. **Respect CLAUDE.md。** 如果配置了 `## Health Stack`，使用那些 exact commands。不要 second-guess。
4. **Skipped is not failed。** 如果 tool 不可用，gracefully skip 并 redistribute weight。不要惩罚 score。
5. **Show raw output for failures。** tool 报告 errors 时，包含实际 output（tail -50），让用户无需重跑也能行动。
6. **Trends require history。** 首次运行时说 "First health check -- no trend data yet. Run /health again after making changes to track progress."
7. **Be honest about scores。** 有 100 个 type errors 但 tests 全通过的 codebase 并不 healthy。composite score 应反映现实。
