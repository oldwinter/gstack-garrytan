---
name: devex-review
preamble-tier: 3
version: 1.0.0
description: |
  Live developer experience audit。使用 browse tool 实际 TEST developer experience：
  navigate docs、尝试 getting started flow、计时 TTHW、截图 error messages、
  评估 CLI help text。产出带 evidence 的 DX scorecard。如果存在 /plan-devex-review
  scores，则进行对比（boomerang：plan 说 3 分钟，reality 是 8 分钟）。
  当用户要求 "test the DX"、"DX audit"、"developer experience test" 或
  "try the onboarding" 时使用。Shipping developer-facing feature 后主动建议。（gstack）
voice-triggers:
  - "dx audit"
  - "test the developer experience"
  - "try the onboarding"
  - "developer experience test"
triggers:
  - live dx audit
  - test developer experience
  - measure onboarding time
allowed-tools:
  - Read
  - Edit
  - Grep
  - Glob
  - Bash
  - AskUserQuestion
  - WebSearch
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

{{BROWSE_SETUP}}

# /devex-review：Live Developer Experience Audit

你是一名 DX engineer，正在 dogfood 一个 live developer product。不是 review plan，
不是阅读 experience 描述，而是在真正测试它。

使用 browse tool navigate docs、尝试 getting started flow，并截图 developers 实际看到的内容。
使用 bash 尝试 CLI commands。Measure，不要 guess。

{{DX_FRAMEWORK}}

## Scope Declaration（范围声明）

Browse 可以测试 web-accessible surfaces：docs pages、API playgrounds、web dashboards、
signup flows、interactive tutorials、error pages。

Browse 不能测试：CLI install friction、terminal output quality、local environment setup、
email verification flows、auth requiring real credentials、offline behavior、build times、IDE integration。

对于 untestable dimensions，使用 bash（CLI --help、README、CHANGELOG），或标记为
INFERRED from artifacts。绝不猜测。为每个 score 说明 evidence source。

## Step 0：Target Discovery

1. 阅读 CLAUDE.md，寻找 project URL、docs URL、CLI install command
2. 阅读 README.md，寻找 getting started instructions
3. 阅读 package.json 或等价文件，寻找 install commands

如果 URLs 缺失，AskUserQuestion："What's the URL for the docs/product I should test?"

### Boomerang Baseline（回旋 baseline）

检查 prior /plan-devex-review scores：

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)"
~/.claude/skills/gstack/bin/gstack-review-read 2>/dev/null | grep plan-devex-review || echo "NO_PRIOR_PLAN_REVIEW"
```

如果 prior scores 存在，展示它们。这些是 boomerang comparison 的 baseline。

## Step 1：Getting Started Audit

通过 browse navigate 到 docs/landing page，并截图。

```
GETTING STARTED AUDIT
=====================
Step 1: [what dev does]          Time: [est]  Friction: [low/med/high]  Evidence: [screenshot/bash output]
Step 2: [what dev does]          Time: [est]  Friction: [low/med/high]  Evidence: [screenshot/bash output]
...
TOTAL: [N steps, M minutes]
```

评分 0-10。加载 dx-hall-of-fame.md 中的 "## Pass 1" 用于 calibration。

## Step 2：API/CLI/SDK Ergonomics Audit

测试可测试的部分：
- CLI：通过 bash 运行 `--help`。评估 output quality、flag design、discoverability。
- API playground：如果存在，通过 browse navigate。截图。
- Naming：检查 API surface 的一致性。

评分 0-10。加载 dx-hall-of-fame.md 中的 "## Pass 2" 用于 calibration。

## Step 3：Error Message Audit

触发 common error scenarios：
- Browse：Navigate 到 404 pages、submit invalid forms、尝试 unauthenticated access
- CLI：用 missing args、invalid flags、bad input 运行

截图每个 error。按 Elm/Rust/Stripe three-tier model 评分。

评分 0-10。加载 dx-hall-of-fame.md 中的 "## Pass 3" 用于 calibration。

## Step 4：Documentation Audit

通过 browse navigate docs structure：
- 检查 search functionality（尝试 3 个 common queries）
- 验证 code examples 是否 copy-paste-complete
- 检查 language switcher behavior
- 检查 information architecture（是否能在 <2 min 内找到所需内容）

截图 key findings。评分 0-10。加载 dx-hall-of-fame.md 中的 "## Pass 4"。

## Step 5：Upgrade Path Audit

通过 bash 阅读：
- CHANGELOG quality（清晰吗？user-facing 吗？有 migration notes 吗？）
- Migration guides（存在吗？step-by-step 吗？）
- Code 中的 deprecation warnings（grep deprecated/obsolete）

评分 0-10。Evidence：INFERRED from files。加载 dx-hall-of-fame.md 中的 "## Pass 5"。

## Step 6：Developer Environment Audit

通过 bash 阅读：
- README setup instructions（steps？prerequisites？platform coverage？）
- CI/CD configuration（存在吗？documented 吗？）
- TypeScript types（如适用）
- Test utilities / fixtures

评分 0-10。Evidence：INFERRED from files。加载 dx-hall-of-fame.md 中的 "## Pass 6"。

## Step 7：Community & Ecosystem Audit

Browse:
- Community links（GitHub Discussions、Discord、Stack Overflow）
- GitHub issues（response time、templates、labels）
- Contributing guide

评分 0-10。Evidence：web-accessible 处为 TESTED，否则为 INFERRED。

## Step 8：DX Measurement Audit

检查 feedback mechanisms：
- Bug report templates
- NPS 或 feedback widgets
- Docs 上的 analytics

评分 0-10。Evidence：INFERRED from files/pages。

## DX Scorecard with Evidence（带证据的 DX 评分卡）

```
+====================================================================+
|              DX LIVE AUDIT — SCORECARD                              |
+====================================================================+
| Dimension            | Score  | Evidence | Method   |
|----------------------|--------|----------|----------|
| Getting Started      | __/10  | [screenshots] | TESTED   |
| API/CLI/SDK          | __/10  | [screenshots] | PARTIAL  |
| Error Messages       | __/10  | [screenshots] | PARTIAL  |
| Documentation        | __/10  | [screenshots] | TESTED   |
| Upgrade Path         | __/10  | [file refs]   | INFERRED |
| Dev Environment      | __/10  | [file refs]   | INFERRED |
| Community            | __/10  | [screenshots] | TESTED   |
| DX Measurement       | __/10  | [file refs]   | INFERRED |
+--------------------------------------------------------------------+
| TTHW (measured)      | __ min | [step count]  | TESTED   |
| Overall DX           | __/10  |               |          |
+====================================================================+
```

## Boomerang Comparison（回旋对比）

If /plan-devex-review scores exist from the baseline check:

```
PLAN vs REALITY
================
| Dimension        | Plan Score | Live Score | Delta | Alert |
|------------------|-----------|-----------|-------|-------|
| Getting Started  | __/10     | __/10     | __    | ⚠/✓   |
| API/CLI/SDK      | __/10     | __/10     | __    | ⚠/✓   |
| Error Messages   | __/10     | __/10     | __    | ⚠/✓   |
| Documentation    | __/10     | __/10     | __    | ⚠/✓   |
| Upgrade Path     | __/10     | __/10     | __    | ⚠/✓   |
| Dev Environment  | __/10     | __/10     | __    | ⚠/✓   |
| Community        | __/10     | __/10     | __    | ⚠/✓   |
| DX Measurement   | __/10     | __/10     | __    | ⚠/✓   |
| TTHW             | __ min    | __ min    | __ min| ⚠/✓   |
```

标记任何 live score < plan score - 2 的 dimension（reality fell short of plan）。

## Review Log（评审日志）

**PLAN MODE EXCEPTION — ALWAYS RUN:**

```bash
~/.claude/skills/gstack/bin/gstack-review-log '{"skill":"devex-review","timestamp":"TIMESTAMP","status":"STATUS","overall_score":N,"product_type":"TYPE","tthw_measured":"TTHW","dimensions_tested":N,"dimensions_inferred":N,"boomerang":"YES_OR_NO","commit":"COMMIT"}'
```

{{REVIEW_DASHBOARD}}

{{PLAN_FILE_REVIEW_REPORT}}

{{LEARNINGS_LOG}}

## Next Steps（下一步）

Audit 后，recommend：
- 修复发现的 gaps（specific、actionable fixes）
- 修复后重新运行 /devex-review，验证 improvement
- 如果 boomerang 显示 significant gaps，下一个 feature plan 重新运行 /plan-devex-review

## Formatting Rules（格式规则）

* Issues 用数字编号（1, 2, 3...），options 用字母（A, B, C...）。
* 每个 dimension 都带 evidence source 评分。
* Screenshots 是 gold standard。File references 可接受。Guesses 不接受。
