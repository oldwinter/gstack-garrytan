## Step 18：Documentation sync（通过 subagent，在 PR creation 前）

**使用 Agent tool 以 `subagent_type: "general-purpose"` 将 /document-release dispatch 为 subagent。**
Subagent 会获得 fresh context window：不会受前 17 steps 的 context rot 影响。它还会运行
**完整** `/document-release` workflow（包含 CHANGELOG clobber protection、doc exclusions、
risky-change gates、named staging、race-safe PR body editing），而不是较弱的 reimplementation。

**Sequencing（顺序）：**此 step 在 Step 17（Push）之后、Step 19（Create PR）之前运行。
PR 会从 final HEAD 创建一次，且 `## Documentation` section 已 baked into initial body。
没有 create-then-re-edit dance。

**Subagent prompt：**

> 你正在 code push 后执行 /document-release workflow。阅读完整 skill file `${HOME}/.claude/skills/gstack/document-release/SKILL.md`，并端到端执行其完整 workflow，包括 CHANGELOG clobber protection、doc exclusions、risky-change gates 和 named staging。不要尝试编辑 PR body，因为 PR 还不存在。Branch：`<branch>`，base：`<base>`。
>
> 完成 workflow 后，在 response 的 LAST LINE 输出单个 JSON object（之后不要再输出其他 text）：
> `{"files_updated":["README.md","CLAUDE.md",...],"commit_sha":"abc1234","pushed":true,"documentation_section":"<markdown block for PR body's ## Documentation section>"}`
>
> 如果没有 documentation files 需要更新，输出：
> `{"files_updated":[],"commit_sha":null,"pushed":false,"documentation_section":null}`

**Parent processing（父流程处理）：**

1. 将 subagent output 的 LAST line parse 为 JSON。
2. 存储 `documentation_section`：Step 19 会将它嵌入 PR body（如果为 null 则省略该 section）。
3. 如果 `files_updated` 非空，打印：`Documentation synced: {files_updated.length} files updated, committed as {commit_sha}`。
4. 如果 `files_updated` 为空，打印：`Documentation is current — no updates needed.`

**如果 subagent fails 或返回 invalid JSON：**打印 warning，并在没有 `## Documentation`
section 的情况下继续 Step 19。不要因 subagent failure 阻塞 /ship。用户可以在 PR lands 后手动运行
`/document-release`。

---

## Step 19：Create PR/MR

**Idempotency check（幂等检查）：**检查此 branch 是否已有 PR/MR。

**如果是 GitHub：**
```bash
gh pr view --json url,number,state -q 'if .state == "OPEN" then "PR #\(.number): \(.url)" else "NO_PR" end' 2>/dev/null || echo "NO_PR"
```

**如果是 GitLab：**
```bash
glab mr view -F json 2>/dev/null | jq -r 'if .state == "opened" then "MR_EXISTS" else "NO_MR" end' 2>/dev/null || echo "NO_MR"
```

如果已经存在 **open** PR/MR：使用 `gh pr edit --body-file "$PR_BODY_FILE"`（GitHub）
或 `glab mr update -d ...`（GitLab）**update** PR body。始终用本次 run 的 fresh results
从零 regenerate PR body（test output、coverage audit、review findings、adversarial review、
TODOS summary、Step 18 的 documentation_section）。绝不复用 prior run 的 stale PR body content。
**Editing 前运行与 create path（Step 19）相同的 redaction scan-at-sink（PR body + title）：
scan temp file，然后从该文件 `gh pr edit --body-file`。**

**始终更新 PR title，使其以 `v$NEW_VERSION` 开头。**PR titles 使用 workspace-aware format
`v<NEW_VERSION> <type>: <summary>`：version 永远在前，无例外，也没有
"custom title kept intentionally" escape hatch。Shared helper `bin/gstack-pr-title-rewrite.sh`
是此 rule 的 single source of truth。

1. 读取 current title：`CURRENT=$(gh pr view --json title -q .title)`（或 `glab mr view -F json | jq -r .title`）。
2. 计算 corrected title：`NEW_TITLE=$(~/.claude/skills/gstack/bin/gstack-pr-title-rewrite.sh "$NEW_VERSION" "$CURRENT")`。helper 处理三种情况：title 已正确（no-op）、title 有不同的 `v<X.Y.Z.W>` prefix（replace）、或 title 没有 version prefix（prepend）。
3. 如果 `NEW_TITLE` 不同于 `CURRENT`，运行 `gh pr edit --title "$NEW_TITLE"`（或 `glab mr update -t "$NEW_TITLE"`）。
4. **Self-check：**re-fetch title，并 assert 它以 `v$NEW_VERSION ` 开头。如果不是，重试一次 edit。如果仍然错误，向用户暴露 failure。

这会在 Step 12 的 queue-drift detection rebumps stale version 时保持 title truthful，
并强制修正此前未按格式创建的 PR。

打印 existing URL 并继续 Step 20。

如果 PR/MR 不存在：使用 Step 0 检测到的平台创建 pull request（GitHub）或 merge request（GitLab）。

PR/MR body 应包含这些 sections：

```
## Summary（摘要）
<总结本次 ship 的全部 substantive changes。运行 `git log <base>..HEAD --oneline` 枚举
每个 commit。排除 VERSION/CHANGELOG metadata commit（那是此 PR 的 bookkeeping，
不是 substantive change）。把剩余 commits 分组成 logical sections（例如
"**Performance**"、"**Dead Code Removal**"、"**Infrastructure**"）。每个 substantive commit
都必须至少出现在一个 section 中。如果某个 commit 的 work 没反映到 summary，
说明你漏掉了。>

## Test Coverage（测试覆盖）
<Step 7 的 coverage diagram，或 "All new code paths have test coverage.">
<如果 Step 7 已运行："Tests: {before} → {after} (+{delta} new)">

## Pre-Landing Review（落地前审查）
<Step 9 code review 的 findings，或 "No issues found.">

## Design Review（设计审查）
<如果 design review 已运行："Design Review (lite): N findings — M auto-fixed, K skipped. AI Slop: clean/N issues.">
<如果没有 frontend files changed："No frontend files changed — design review skipped.">

## Eval Results（eval 结果）
<如果 evals 已运行：suite names、pass/fail counts、cost dashboard summary。如果 skipped："No prompt-related files changed — evals skipped.">

## Greptile Review（Greptile 审查）
<如果找到 Greptile comments：bullet list，每条带 [FIXED] / [FALSE POSITIVE] / [ALREADY FIXED] tag + 一行 summary>
<如果没有 Greptile comments："No Greptile comments.">
<如果 Step 10 时 PR 不存在：完全 omit this section>

## Scope Drift（范围漂移）
<如果 scope drift 已运行："Scope Check: CLEAN" 或 drift/creep findings list>
<如果没有 scope drift：omit this section>

## Plan Completion（计划完成度）
<如果找到 plan file：Step 8 的 completion checklist summary>
<如果没有 plan file："No plan file detected.">
<如果有 plan items deferred：列出 deferred items>

## Linked Spec（关联 spec）
<Auto-detect：通过以下命令查找匹配此 branch 的 /spec archives：
  eval "$(${ctx.paths.binDir}/gstack-paths)"
  eval "$(${ctx.paths.binDir}/gstack-slug)"
  CURRENT_BRANCH=$(git branch --show-current)
  SPEC_ARCHIVES="$GSTACK_STATE_ROOT/projects/$SLUG/specs"
  # 找到 spec_branch frontmatter 匹配 current branch 的 newest archive（或其 parent 之一；
  # 如果 spec spawned worktree spec/<slug>-$$，该 spawned worktree 就是 /ship 运行处）。
  SPEC_FILE=$(grep -l "^spec_branch: $CURRENT_BRANCH$" "$SPEC_ARCHIVES"/*.md 2>/dev/null | head -1)
  [ -z "$SPEC_FILE" ] && exit  # no spec；完全 omit this section
  SPEC_ISSUE=$(grep "^spec_issue_number:" "$SPEC_FILE" | cut -d' ' -f2)
  [ -z "$SPEC_ISSUE" ] && exit  # spec archive 存在但无 issue number；omit

  # CONDITIONAL Closes #N (codex F4)：只在上方 Plan Completion 为 "complete" 时添加。
  # 如果 Step 8 的 plan completion gate 报告任何 deferred 或 failed items，emit：
  #   "Linked to #$SPEC_ISSUE (partial delivery — NOT auto-closing; close manually after follow-up)"
  # 如果 Plan Completion fully complete，emit：
  #   "Closes #$SPEC_ISSUE"
  # 并在 PR body 中包含 Closes #N line，让 GitHub 在 merge 时 auto-close。>

<Format:
  Closes #<N>

  This PR delivers the spec at <archive path relative to repo root>.
  Spec filed: <spec_filed_at from frontmatter>>

<如果是 partial delivery，改为 emit：
  Linked to #<N> (partial delivery — not auto-closing).
  Deferred items: <来自 Plan Completion 的 list>.
  Close #<N> manually after follow-up lands.>

<如果没有 /spec archive 匹配此 branch：完全 omit this section。>

## Verification Results（验证结果）
<如果 verification 已运行：Step 8.1 的 summary（N PASS, M FAIL, K SKIPPED）>
<如果 skipped：reason（no plan, no server, no verification section）>
<如果 not applicable：omit this section>

## TODOS（待办）
<如果 items marked complete：带 version 的 completed items bullet list>
<如果没有 items completed："No TODO items completed in this PR.">
<如果 TODOS.md created 或 reorganized：说明这一点>
<如果 TODOS.md 不存在且用户 skipped：omit this section>

## Documentation（文档）
<在这里原样嵌入 Step 18 subagent 返回的 `documentation_section` string。>
<如果 Step 18 返回 `documentation_section: null`（no docs updated），完全 omit this section。>

## Test plan（测试计划）
- [x] All Rails tests pass (N runs, 0 failures)
- [x] All Vitest tests pass (N tests)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

#### Redaction scan（PR body + title）：create 和 edit 前都运行

Public repo 上的 PR body 是 world-readable。发送前 scan-at-sink：将 composed body
写入 temp file，用 shared engine 扫描那个 file，并将同一个 file 传给 `gh`/`glab`。
将任何 Codex / Greptile / eval output sections 包在 tool-attributed fences
（` ```codex-review ` / ` ```greptile `）中，让 engine 对这些 tools quote 的 example credentials
降级为 WARN，而不是阻塞 PR（fence 内的 live-format credential 仍会 block）。

```bash
REDACT_VIS=$(~/.claude/skills/gstack/bin/gstack-config get redact_repo_visibility 2>/dev/null)
[ -z "$REDACT_VIS" ] && REDACT_VIS=$(gh repo view --json visibility -q .visibility 2>/dev/null | tr 'A-Z' 'a-z')
REDACT_VIS="${REDACT_VIS:-unknown}"
PR_BODY_FILE=$(mktemp)
cat > "$PR_BODY_FILE" <<'PR_BODY_EOF'
<PR body from above>
PR_BODY_EOF
~/.claude/skills/gstack/bin/gstack-redact --from-file "$PR_BODY_FILE" --repo-visibility "$REDACT_VIS" --self-email "$(git config user.email 2>/dev/null)" --json
case $? in
  3) echo "BLOCKED — credential in PR body. Rotate + redact, do not create the PR."; exit 1 ;;
  2) echo "MEDIUM findings — confirm per finding (sterner on public) before proceeding." ;;
esac
# Also scan the title (short, single-line):
printf '%s' "v$NEW_VERSION <type>: <summary>" | ~/.claude/skills/gstack/bin/gstack-redact --repo-visibility "$REDACT_VIS" --json
```

HIGH blocks（exit 3，无 skip）。MEDIUM -> AskUserQuestion（PII subset 提供 `--auto-redact`）。
同一 scan 会在 `gh pr edit --body` path（Step 17）前运行。

**如果是 GitHub：**从 SCANNED file 创建（scanned 的 exact bytes = sent 的 bytes）：

```bash
# PR title MUST start with v$NEW_VERSION — enforced on every run, no exceptions.
# (See Step 19 idempotency block + bin/gstack-pr-title-rewrite.sh for the rule.)
gh pr create --base <base> --title "v$NEW_VERSION <type>: <summary>" --body-file "$PR_BODY_FILE"
rm -f "$PR_BODY_FILE"
```

**如果是 GitLab：**

```bash
# MR title MUST start with v$NEW_VERSION — enforced on every run, no exceptions.
# (See Step 19 idempotency block + bin/gstack-pr-title-rewrite.sh for the rule.)
glab mr create -b <base> -t "v$NEW_VERSION <type>: <summary>" -d "$(cat <<'EOF'
<MR body from above>
EOF
)"
```

**如果两个 CLI 都不可用：**
打印 branch name、remote URL，并指示用户通过 web UI 手动创建 PR/MR。不要停止：
code 已 push 且 ready。

**输出 PR/MR URL**，然后继续 Step 20。

---
