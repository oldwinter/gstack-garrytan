---
name: ios-sync
preamble-tier: 3
version: 1.0.0
description: |
  基于最新 upstream gstack templates 重新生成 iOS debug bridge。更新
  StateServer.swift、DebugOverlay.swift、Package.swift，以及 typed @Observable
  state accessors。在升级 gstack 后，或添加需要 accessor coverage 的新
  ViewModels/properties 后使用。当用户要求 "resync the iOS debug bridge"、
  "regenerate iOS accessors" 或 "update the gstack iOS instrumentation" 时使用。（gstack）
voice-triggers:
  - "resync the iOS debug bridge"
  - "regenerate iOS accessors"
  - "update the gstack iOS instrumentation"
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - AskUserQuestion
triggers:
  - resync the ios debug bridge
  - regenerate ios accessors
  - update the gstack ios instrumentation
---

{{PREAMBLE}}

# Resync iOS debug bridge（重新同步 iOS debug bridge）

`/ios-qa` 安装到 app 后，用户可能会：

1. 添加需要 accessor coverage 的新 `@Observable` classes 或 properties。
2. 将 gstack 升级到带 hardening fixes 的新版本。
3. 将 `@Snapshotable` marker 移动到不同 field。

此 skill 会在原地 regenerate 相关 artifacts。

**Templates 位于 upstream gstack。**此 skill 从
`~/.claude/skills/gstack/ios-qa/templates/` resolve 它们（开发 gstack 本身时使用 worktree 的
`ios-qa/templates/`）。Fork 的 HTTP-fetch pattern 已移除。

## Phase 1：检测已安装版本

1. 读取 `<app>/DebugBridgeGenerated/.gstack-version`（由 /ios-qa 在 install 时写入）。
   如果缺失，将 install 视为 "unknown old version"。
2. 从 `$GSTACK_HOME/ios-qa/.gstack-version` 读取 upstream version（或 installed gstack binary
   中 baked 的值）。
3. 如果 versions match 且没有新增 `@Observable` classes，则 early exit："already up to date"。

## Phase 2：重新生成 codegen output

运行 `gstack-ios-qa-regen`（或直接运行底层 SwiftPM tool）：

```bash
swift run --package-path "$GSTACK_HOME/ios-qa/scripts/gen-accessors-tool" \
  gen-accessors --input "$APP_SOURCE_DIR" --output "$APP_SOURCE_DIR/DebugBridgeGenerated"
```

Composite-hash cache key 会判断是否真的需要 regenerating；如果 Swift version、generator git rev、
lockfile、source content 和 platform triple 都匹配 cache，这就是约 50ms 的 no-op。

## Phase 3：原地更新 templated Swift files

对每个来自 `ios-qa/templates/*.swift.template` 的 file：

1. 读取当前 installed file：
   `<app>/DebugBridgeGenerated/<Name>.swift`.
2. 读取 upstream template：
   `$GSTACK_HOME/ios-qa/templates/<Name>.swift.template`.
3. 如果 installed file 有 `// GSTACK-EDIT-LINE` marker，将用户 edits fold forward。
4. 否则，用 new template 直接 replace 该 file（diff non-trivial 时先 AskUserQuestion）。

## Phase 4：Verify

1. 针对 app package 的 `swift build` succeeds。
2. `xcodebuild -scheme <SchemeName>` succeeds。
3. 在 device 上 re-launch app；daemon connects + rotates token。
4. `GET /state/snapshot` 返回新的 accessor schema hash。

## Failure modes（失败模式）

| 症状 | 处理方式 |
|---|---|
| Swift compile fails after regen | 通过 `git restore` revert，并用 AskUserQuestion 呈现 compile error |
| Schema hash unchanged after adding new @Observable | 新 class 没有标记 `@Snapshotable`，codegen 正确排除了它。如果用户想把它纳入 snapshot，添加 wrapper。 |
| `--input` source dir contains test fixtures | gen-accessors 会递归扫描 input dir；通过 `--exclude` 排除 test/ |
