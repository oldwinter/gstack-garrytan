---
name: ios-fix
preamble-tier: 3
version: 1.0.0
description: |
  Autonomous iOS bug fixer。接收 /ios-qa 发现的 bug，读取 source、写入 fix、
  rebuild、redeploy，并在真实 device 上验证 fix。闭环：find bug -> fix bug -> confirm fix，
  零 human intervention。捕获 pre-bug state snapshot 作为 regression test fixture，
  让 bug 不能 silent recur。当 /ios-qa 报告 bug 且你希望自动修复，或用户要求
  "fix this iOS bug"、"patch the iPhone app"、"auto-fix the iOS issue" 时使用。（gstack）
voice-triggers:
  - "fix the iOS bug"
  - "patch the iPhone app"
  - "auto-fix the iOS issue"
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Grep
  - Glob
  - AskUserQuestion
triggers:
  - fix this ios bug
  - patch the iphone app
  - auto-fix the ios issue
---

{{PREAMBLE}}

# Autonomous iOS bug fixer（自动 iOS bug 修复器）

## Iron Law（铁律）

**没有 REPRODUCING SNAPSHOT 就不修。**编辑任何 Swift source 前，agent 必须捕获一个能 reproduce bug 的
`GET /state/snapshot`。该 snapshot 会成为 regression test fixture（`test/fixtures/ios-fix/`）。
没有 reproducing snapshot 就落地的 fix，三个月后还会再修一次。

## Phase 1：Reproduce bug

1. 阅读 `/ios-qa` finding（bug description、screenshot、suspected accessibility-tree node）。
2. 通过 `POST /tap`、`/swipe`、`/type` 或 `POST /state/<key>`（仅 snapshot-eligible fields）
   将 device 带入 bug state。
3. 捕获 `GET /state/snapshot` -> 写入
   `test/fixtures/ios-fix/<bug-slug>-pre.json`.
4. 捕获 `GET /screenshot` -> 写入
   `test/fixtures/ios-fix/<bug-slug>-pre.png`.
5. 持久化一行 what's wrong + expected behavior 描述。

## Phase 2：Locate root cause

按 `/investigate` 的 Iron Law：没有 root cause 就不修。Agent 阅读 Swift source，
从 buggy screen 回溯到 view model、data flow 和 state mutation。识别修复 behavior 的最小变更。

如果有多个 plausible root causes，使用 AskUserQuestion：让用户选择要修哪一个。

## Phase 3：Apply fix

1. 编辑 Swift source。保持 diff minimal。
2. Rebuild：`xcodebuild -scheme <SchemeName>
   -destination 'platform=iOS,id=<UDID>' build install`.
3. Daemon 检测 rebuild，并 reconnect StateServer tunnel。
4. Re-deploy。同一个 boot-token rotation flow 会运行。

## Phase 4：Verify

1. 用 pre-bug snapshot 调用 `POST /state/restore` -> reproduce state。
2. 获取 fresh screenshot。与
   `test/fixtures/ios-fix/<bug-slug>-pre.png`.
   比较。
3. 如果 bug visibly persists，说明 fix 无效：revert 并重试（最多 3 iterations，然后 escalate 给用户）。
4. 如果 bug 消失，捕获 `<bug-slug>-post.png` 供 regression test 使用。

## Phase 5：添加 regression test

在 `test/fixtures/ios-fix/<bug-slug>.test.ts` 中写 test：

1. 加载 pre-bug snapshot。
2. 通过 `POST /state/restore` restore 它。
3. 在 real device 上 assert post-fix behavior（gated
   `GSTACK_HAS_IOS_DEVICE=1`, periodic tier).

将 snapshot fixture + test file 与 fix 一起 commit。

## Failure modes（失败模式）

| Symptom（现象） | Action（处理） |
|---|---|
| 3 iterations, bug still present | STOP, report to user with current best hypothesis |
| `409 schema_mismatch` on /state/restore after rebuild | Re-codegen accessors (`swift run gen-accessors`), re-snapshot |
| Device disconnects mid-fix | Daemon auto-reconnects; resume from Phase 4 |
| Build fails | Revert Swift edits; investigate compile error before re-applying fix |
