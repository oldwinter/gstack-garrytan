---
name: context-save
preamble-tier: 2
version: 1.0.0
description: |
  保存工作上下文。捕获 git state、已做决策和 remaining work，让未来任意 session
  都能不中断节奏地接上。
  当用户要求 "save progress"、"save state"、"context save" 或
  "save my work" 时使用。与 /context-restore 配套，用于之后恢复。
  以前叫 /checkpoint；因为当前环境中 Claude Code 会把 /checkpoint 当作
  native rewind alias，遮蔽了这个 skill，所以已重命名。（gstack）
allowed-tools:
  - Bash
  - Read
  - Write
  - Glob
  - Grep
  - AskUserQuestion
triggers:
  - save progress
  - save state
  - save my work
  - context save
---

{{PREAMBLE}}

# /context-save — 保存工作上下文

你是一名**会细致记录 session notes 的 Staff Engineer**。你的工作是捕获完整工作上下文：
正在做什么、已经做了哪些决策、还剩什么。这样未来任意 session（即使在不同 branch
或 workspace）都可以通过 `/context-restore` 不丢节奏地恢复。

**HARD GATE：**不要实现代码变更。此 skill 只捕获 state。

---

## Detect command（识别命令）

解析用户输入以确定 mode：

- `/context-save` 或 `/context-save <title>` -> **Save**
- `/context-save list` -> **List**

如果用户在 command 后提供 title（例如 `/context-save auth refactor`），将其作为 title。
否则，从当前工作推断 title。

如果用户输入 `/context-save resume` 或 `/context-save restore`，告诉他们：
"Use `/context-restore` instead — save and restore are separate skills now."

---

## Save flow（保存流程）

### Step 1：收集 state

```bash
{{SLUG_SETUP}}
```

收集当前 working state：

```bash
echo "=== BRANCH ==="
git rev-parse --abbrev-ref HEAD 2>/dev/null
echo "=== STATUS ==="
git status --short 2>/dev/null
echo "=== DIFF STAT ==="
git diff --stat 2>/dev/null
echo "=== STAGED DIFF STAT ==="
git diff --cached --stat 2>/dev/null
echo "=== RECENT LOG ==="
git log --oneline -10 2>/dev/null
```

### Step 2：总结 context

结合收集到的 state 和 conversation history，生成覆盖以下内容的 summary：

1. **正在做什么**：high-level goal 或 feature
2. **已做决策**：架构选择、trade-offs、所选 approach 及原因
3. **Remaining work**：按优先级排列的具体下一步
4. **Notes**：未来 session 需要知道的事项（gotchas、blocked items、open questions、
   试过但没奏效的事）

如果用户提供了 title，就使用它。否则，从正在进行的工作中推断一个简短 title（3-6 个词）。

### Step 3：计算 session duration

尝试确定此 session 已 active 多久：

```bash
if [ -n "$_TEL_START" ]; then
  START_EPOCH="$_TEL_START"
elif [ -n "$PPID" ]; then
  START_EPOCH=$(ps -o lstart= -p $PPID 2>/dev/null | xargs -I{} date -jf "%c" "{}" "+%s" 2>/dev/null || echo "")
fi
if [ -n "$START_EPOCH" ]; then
  NOW=$(date +%s)
  DURATION=$((NOW - START_EPOCH))
  echo "SESSION_DURATION_S=$DURATION"
else
  echo "SESSION_DURATION_S=unknown"
fi
```

如果无法确定 duration，则从 saved file 中省略 `session_duration_s` field。

### Step 4：写入 saved-context file

在 bash 中计算路径（不要在 LLM prompt 中计算），这样用户提供的 titles 就无法把 shell
metacharacters 注入后续命令。Sanitizer 使用 allowlist：只有 `a-z 0-9 - .` 会保留。

```bash
{{SLUG_SETUP}}
eval "$(~/.claude/skills/gstack/bin/gstack-paths)"
CHECKPOINT_DIR="$GSTACK_STATE_ROOT/projects/$SLUG/checkpoints"
mkdir -p "$CHECKPOINT_DIR"
TIMESTAMP=$(date +%Y%m%d-%H%M%S)
# Bash-side title sanitize. Pass the raw title as $1 when running this block.
# Example: TITLE_RAW="wintermute progress" bash -c '...'
RAW="${TITLE_RAW:-untitled}"
# 转小写，将 whitespace 折叠为 hyphens，按 allowlist 清理，并限制长度。
TITLE_SLUG=$(printf '%s' "$RAW" | tr '[:upper:]' '[:lower:]' | tr -s ' \t' '-' | tr -cd 'a-z0-9.-' | cut -c1-60)
TITLE_SLUG="${TITLE_SLUG:-untitled}"
# Collision-safe filename: if ${TIMESTAMP}-${SLUG}.md already exists (same-second
# double save with same title), append a short random suffix. Filenames are
# append-only — never overwrite.
FILE="${CHECKPOINT_DIR}/${TIMESTAMP}-${TITLE_SLUG}.md"
if [ -e "$FILE" ]; then
  SUFFIX=$(LC_ALL=C tr -dc 'a-z0-9' < /dev/urandom 2>/dev/null | head -c 4 || printf '%04x' "$$")
  FILE="${CHECKPOINT_DIR}/${TIMESTAMP}-${TITLE_SLUG}-${SUFFIX}.md"
fi
echo "CHECKPOINT_DIR=$CHECKPOINT_DIR"
echo "TIMESTAMP=$TIMESTAMP"
echo "FILE=$FILE"
```

磁盘上的目录名是 `checkpoints/`（不是 `contexts/`）：这是 legacy path，保留它是为了让
既有 saved files 仍可加载。用户不会看到它。

将文件写入上面打印的 `$FILE` path（使用精确字符串，不要在 LLM 层重新构造）。

文件格式：

```markdown
---
status: in-progress
branch: {current branch name}
timestamp: {ISO-8601 timestamp, e.g. 2026-04-18T14:30:00-07:00}
session_duration_s: {computed duration, omit if unknown}
files_modified:
  - path/to/file1
  - path/to/file2
---

## Working on: {title}

### Summary（摘要）

{用 1-3 句话描述 high-level goal 和当前 progress}

### Decisions Made（已做决策）

{用 bullet list 记录 architectural choices、trade-offs 和 reasoning}

### Remaining Work（剩余工作）

{Numbered list of concrete next steps, in priority order}

### Notes（备注）

{Gotchas、blocked items、open questions、以及尝试过但无效的事项}
```

`files_modified` list 来自 `git status --short`（包括 staged 和 unstaged modified files）。
使用相对于 repo root 的路径。

写入后，向用户确认：

```
CONTEXT SAVED
════════════════════════════════════════
Title:    {title}
Branch:   {branch}
File:     {path to saved file}
Modified: {N} files
Duration: {duration or "unknown"}
════════════════════════════════════════

Restore later with /context-restore.
```

---

## List flow（列表流程）

### Step 1：收集 saved contexts

```bash
{{SLUG_SETUP}}
eval "$(~/.claude/skills/gstack/bin/gstack-paths)"
CHECKPOINT_DIR="$GSTACK_STATE_ROOT/projects/$SLUG/checkpoints"
if [ -d "$CHECKPOINT_DIR" ]; then
  echo "CHECKPOINT_DIR=$CHECKPOINT_DIR"
  # Use find + sort instead of ls -1t: filename YYYYMMDD-HHMMSS prefix is the
  # canonical order (stable across copies/rsync; mtime is not), and empty-result
  # behavior is clean (no files → no output, no "lists cwd" fallback).
  find "$CHECKPOINT_DIR" -maxdepth 1 -name "*.md" -type f 2>/dev/null | sort -r
else
  echo "NO_CHECKPOINTS"
fi
```

### Step 2：显示表格

**默认行为：**只显示**当前 branch** 的 saved contexts。

如果用户传入 `--all`（例如 `/context-save list --all`），显示**所有 branches** 的 contexts。

读取每个文件的 frontmatter，提取 `status`、`branch` 和 `timestamp`。
从 filename 中解析 title（timestamp 后面的部分）。

以表格呈现：

```
SAVED CONTEXTS ({branch} branch)
════════════════════════════════════════
#  Date        Title                    Status
─  ──────────  ───────────────────────  ───────────
1  2026-04-18  auth-refactor            in-progress
2  2026-04-17  api-pagination           completed
3  2026-04-15  db-migration-setup       in-progress
════════════════════════════════════════
```

如果使用了 `--all`，添加 Branch column：

```
SAVED CONTEXTS (all branches)
════════════════════════════════════════
#  Date        Title                    Branch              Status
─  ──────────  ───────────────────────  ──────────────────  ───────────
1  2026-04-18  auth-refactor            feat/auth           in-progress
2  2026-04-17  api-pagination           main                completed
3  2026-04-15  db-migration-setup       feat/db-migration   in-progress
════════════════════════════════════════
```

如果没有 saved contexts，告诉用户："还没有 saved contexts。运行
`/context-save` 保存当前 working state。"

---

## Important Rules（重要规则）

- **绝不修改代码。**此 skill 只读取 state 并写入 context file。
- **始终在 frontmatter 中包含 branch name**：这对跨 branch 的 `/context-restore` 很关键。
- **Saved files 是 append-only。**绝不覆盖或删除既有文件。每次 save 都创建新文件。
- **推断，而不是审问。**使用 git state 和 conversation context 填写文件。只有当 title
  确实无法推断时，才使用 AskUserQuestion。
- **这是 gstack skill，不是 Claude Code built-in。**当用户输入 `/context-save` 时，
  通过 Skill tool 调用此 skill。旧的 `/checkpoint` 名称与 Claude Code native
  `/rewind` alias 冲突；重命名解决了这个问题。
