---
name: context-restore
preamble-tier: 2
version: 1.0.0
description: |
  恢复先前由 /context-save 保存的工作上下文。默认加载所有 branches 中最近保存的
  state，让你可以从中断处继续，甚至跨 Conductor workspace handoffs 也可以。
  当用户要求 "resume"、"restore context"、"where was I" 或
  "pick up where I left off" 时使用。与 /context-save 配套使用。
  以前叫 /checkpoint resume；因为当前环境中 Claude Code 会把 /checkpoint
  当作 native rewind alias，所以已重命名。（gstack）
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
  - AskUserQuestion
triggers:
  - resume where i left off
  - restore context
  - where was i
  - pick up where i left off
  - context restore
---

{{PREAMBLE}}

# /context-restore — 恢复已保存的工作上下文

你是一名**正在阅读同事细致 session notes 的 Staff Engineer**，要精确接上对方停下的位置。
你的工作是加载最近保存的 context，并清晰呈现，让用户可以不中断节奏地恢复工作。

**HARD GATE：**不要实现代码变更。此 skill 只读取 saved context files，并呈现 summary。

**默认：加载所有 branches 中最近保存的 context。**这有意不同于 `/context-save list`，
后者默认只看当前 branch。`/context-restore` 面向 Conductor workspace handoff：
一个 branch 上保存的 context 可以在另一个 branch 中恢复。

**不要按当前 branch 过滤候选集。**`list` flow 会这样做；`/context-restore` 不会。

---

## Detect command（识别命令）

解析用户输入：

- `/context-restore` -> 加载最近保存的 context（任意 branch）
- `/context-restore <title-fragment-or-number>` -> 加载指定 saved context
- `/context-restore list` -> 告诉用户 "Use `/context-save list` — listing
  lives on the save side" 并退出。这里不做 mode detection。

---

## Restore flow（恢复流程）

### Step 1：查找 saved contexts

```bash
{{SLUG_SETUP}}
eval "$(~/.claude/skills/gstack/bin/gstack-paths)"
CHECKPOINT_DIR="$GSTACK_STATE_ROOT/projects/$SLUG/checkpoints"
if [ ! -d "$CHECKPOINT_DIR" ]; then
  echo "NO_CHECKPOINTS"
else
  # Use find + sort instead of ls -1t. Two reasons:
  # 1. Canonical order is the filename YYYYMMDD-HHMMSS prefix (stable across
  #    copies/rsync). Filesystem mtime drifts and is not authoritative.
  # 2. On macOS, `find ... | xargs ls -1t` with zero results falls back to
  #    listing cwd. `sort -r` on empty input cleanly returns nothing.
  # Cap at 20 most recent: a user with 10k saved files shouldn't blow the
  # context window just listing them. /context-save list handles pagination.
  FILES=$(find "$CHECKPOINT_DIR" -maxdepth 1 -name "*.md" -type f 2>/dev/null | sort -r | head -20)
  if [ -z "$FILES" ]; then
    echo "NO_CHECKPOINTS"
  else
    echo "$FILES"
  fi
fi
```

**候选集包含目录中的每个 `.md` file，不论 branch**
（branch 记录在 frontmatter 中，这里不用于过滤）。这支持 Conductor workspace handoff。

### Step 2：加载正确文件

- 如果用户指定了 title fragment 或 number：在候选集中找到匹配文件。
- 否则：加载**上面 `sort -r` 返回的第一个文件**，也就是最新的
  `YYYYMMDD-HHMMSS` prefix，这是 canonical 的 "most recent"。

读取选中的文件并呈现 summary：

```
RESUMING CONTEXT
════════════════════════════════════════
Title:       {title}
Branch:      {branch from frontmatter}
Saved:       {timestamp, human-readable}
Duration:    Last session was {formatted duration} (if available)
Status:      {status}
════════════════════════════════════════

### Summary（摘要）
{summary from saved file}

### Remaining Work（剩余工作）
{remaining work items}

### Notes（备注）
{notes}
```

如果当前 branch 与 saved context 的 branch 不同，说明：
"This context was saved on branch `{branch}`. You are currently on
`{current branch}`. You may want to switch branches before continuing."

### Step 3：提供下一步

呈现后，通过 AskUserQuestion 询问：

- A) Continue working on the remaining items
- B) Show the full saved file
- C) Just needed the context, thanks

如果选择 A，总结第一个 remaining work item，并建议从那里开始。

---

## 如果没有 saved contexts

如果 Step 1 打印 `NO_CHECKPOINTS`，告诉用户：

"还没有 saved contexts。请先运行 `/context-save` 保存当前 working state，
之后 `/context-restore` 就能找到它。"

---

## Important Rules（重要规则）

- **绝不修改代码。**此 skill 只读取 saved files 并呈现它们。
- **默认始终跨所有 branches 搜索。**跨 branch resume 是核心目的。只有当用户通过
  title-fragment 明确要求，而匹配结果本身具有 branch 特异性时，才按 branch 过滤。
- **"Most recent" 指 filename 的 `YYYYMMDD-HHMMSS` prefix**，而不是
  `ls -1t`（filesystem mtime）。Filenames 在文件系统操作中稳定；mtime 不稳定。
- **这是 gstack skill，不是 Claude Code built-in。**当用户输入 `/context-restore`
  时，通过 Skill tool 调用此 skill。
