---
name: setup-browser-cookies
preamble-tier: 1
version: 1.0.0
description: |
  将你真实 Chromium browser 中的 cookies 导入 headless browse session。
  打开 interactive picker UI，让你选择要导入哪些 cookie domains。
  在 QA testing authenticated pages 前使用。当用户要求 "import cookies"、
  "login to the site" 或 "authenticate the browser" 时使用。（gstack）
triggers:
  - import browser cookies
  - login to test site
  - setup authenticated session
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---

{{PREAMBLE}}

# Setup Browser Cookies（设置浏览器 cookies）

将真实 Chromium browser 中的 logged-in sessions 导入 headless browse session。

## CDP mode check（CDP mode 检查）

首先，检查 browse 是否已经连接到用户的真实 browser：
```bash
$B status 2>/dev/null | grep -q "Mode: cdp" && echo "CDP_MODE=true" || echo "CDP_MODE=false"
```
如果 `CDP_MODE=true`：告诉用户 "不需要导入：你已经通过 CDP 连接到真实 browser，cookies 和 sessions 已可用。" 然后停止。不需要 cookie import。

## How it works（工作方式）

1. 找到 browse binary
2. 运行 `cookie-import-browser`，检测 installed browsers 并打开 picker UI
3. 用户在 browser 中选择要导入哪些 cookie domains
4. Cookies 被 decrypt 并 loaded into Playwright session

## Steps（步骤）

### 1. 查找 browse binary

{{BROWSE_SETUP}}

### 2. 打开 cookie picker

```bash
$B cookie-import-browser
```

这会 auto-detect installed Chromium browsers，并在 default browser 中打开 interactive picker UI。
你可以：
- 在 installed browsers 之间切换
- Search domains
- 点击 "+" 导入某个 domain 的 cookies
- 点击 trash 移除 imported cookies

告诉用户：**"Cookie picker 已打开：请在 browser 中选择要导入的 domains，完成后告诉我。"**

### 3. Direct import（alternative）

如果用户直接指定 domain（例如 `/setup-browser-cookies github.com`），跳过 UI：

```bash
$B cookie-import-browser comet --domain github.com
```

如果指定了 browser，用相应 browser 替换 `comet`。

### 4. Verify（验证）

用户确认完成后：

```bash
$B cookies
```

向用户展示 imported cookies summary（domain counts）。

## Notes（备注）

- 在 macOS 上，每个 browser 的首次 import 可能触发 Keychain dialog：点击 "Allow" / "Always Allow"
- 在 Linux 上，`v11` cookies 可能需要 `secret-tool`/libsecret access；`v10` cookies 使用 Chromium 的 standard fallback key
- Cookie picker 与 browse server 使用同一 port（无额外 process）
- UI 中只显示 domain names 和 cookie counts：不暴露 cookie values
- Browse session 会在 commands 之间持久化 cookies，因此 imported cookies 会立即生效
