---
name: gstack-contrib-add-host
description: |
  Contributor-only skill：为 gstack 的 multi-host system 创建新的 host config。
  不会为 end users 安装。只能从 gstack source repo 使用。
triggers:
  - add new host
  - create host config
  - contribute new agent host
---

# /gstack-contrib-add-host — 添加新 Host

这个 skill 帮助 contributors 为 gstack 添加新的 AI coding agent 支持。

## 你将创建什么

一个位于 `hosts/<name>.ts` 的 TypeScript 文件，定义：
- 用于检测的 CLI binary name
- Skill directory paths（global + local）
- Frontmatter transformation rules
- Path 和 tool rewrites
- Runtime root symlink manifest

## 步骤

### 1. 收集 host 信息

询问 contributor：
- Agent 名称是什么？（例如 "OpenCode"）
- CLI binary 是什么？（例如 "opencode"）
- 它把 global skills 存在哪里？（例如 "~/.config/opencode/skills/"）
- 它把 project-local skills 存在哪里？（例如 ".opencode/skills/"）
- 它支持哪些 frontmatter fields？（最少需要 name + description）
- 它是否有自己的 tool names？（例如用 "exec" 替代 "Bash"）

### 2. 创建 config 文件

以 `hosts/opencode.ts` 为参考。用收集到的信息创建 `hosts/<name>.ts`。
遵循 `scripts/host-config.ts` 中的 HostConfig interface。

### 3. 在 index 中注册

在 `hosts/index.ts` 中添加 import 和 re-export。

### 4. 添加到 .gitignore

把 `.<name>/` 添加到 `.gitignore`。

### 5. 生成并验证

```bash
bun run gen:skill-docs --host <name>
```

检查：
- Output 存在于 `.<name>/skills/gstack-*/SKILL.md`
- 没有 `.claude/skills` path leakage
- Frontmatter 符合 expected format

### 6. 运行 tests

```bash
bun test test/gen-skill-docs.test.ts
```

所有 parameterized tests 会自动包含 new host。
