---
name: gstack-upgrade
version: 1.1.0
description: |
  将 gstack 升级到最新版本。检测 global vs vendored install，执行 upgrade，
  并展示 what's new。当用户要求 "upgrade gstack"、"update gstack" 或
  "get latest version" 时使用。
voice-triggers:
  - "upgrade the tools"
  - "update the tools"
  - "gee stack upgrade"
  - "g stack upgrade"
triggers:
  - upgrade gstack
  - update gstack version
  - get latest gstack
allowed-tools:
  - Bash
  - Read
  - Write
  - AskUserQuestion
---

# /gstack-upgrade

将 gstack 升级到最新版本，并展示 what's new。

## Inline upgrade flow（内联升级流程）

所有 skill preambles 在检测到 `UPGRADE_AVAILABLE` 时都会引用本 section。

### Step 1：询问用户（或 auto-upgrade）

首先，检查 auto-upgrade 是否启用：
```bash
_AUTO=""
[ "${GSTACK_AUTO_UPGRADE:-}" = "1" ] && _AUTO="true"
[ -z "$_AUTO" ] && _AUTO=$(~/.claude/skills/gstack/bin/gstack-config get auto_upgrade 2>/dev/null || true)
echo "AUTO_UPGRADE=$_AUTO"
```

**如果 `AUTO_UPGRADE=true` 或 `AUTO_UPGRADE=1`：**跳过 AskUserQuestion。记录 "Auto-upgrading gstack v{old} -> v{new}..."，并直接进入 Step 2。如果 auto-upgrade 期间 `./setup` 失败，从 backup（`.bak` directory）恢复，并提醒用户："Auto-upgrade failed — restored previous version. Run `/gstack-upgrade` manually to retry."

**否则**，使用 AskUserQuestion：
- Question: "gstack **v{new}** 可用（你当前是 v{old}）。现在 upgrade 吗？"
- Options: ["是，现在 upgrade", "始终让我保持最新", "暂不", "不要再询问"]

**如果 "是，现在 upgrade"：**进入 Step 2。

**如果 "始终让我保持最新"：**
```bash
~/.claude/skills/gstack/bin/gstack-config set auto_upgrade true
```
告诉用户："Auto-upgrade 已启用。未来 updates 会自动 install。" 然后进入 Step 2。

**如果 "暂不"：**写入 snooze state，并使用递增 backoff（第一次 snooze = 24h，第二次 = 48h，第三次及以后 = 1 week），然后继续当前 skill。不要再次提及 upgrade。
```bash
_SNOOZE_FILE="$HOME/.gstack/update-snoozed"
_REMOTE_VER="{new}"
_CUR_LEVEL=0
if [ -f "$_SNOOZE_FILE" ]; then
  _SNOOZED_VER=$(awk '{print $1}' "$_SNOOZE_FILE")
  if [ "$_SNOOZED_VER" = "$_REMOTE_VER" ]; then
    _CUR_LEVEL=$(awk '{print $2}' "$_SNOOZE_FILE")
    case "$_CUR_LEVEL" in *[!0-9]*) _CUR_LEVEL=0 ;; esac
  fi
fi
_NEW_LEVEL=$((_CUR_LEVEL + 1))
[ "$_NEW_LEVEL" -gt 3 ] && _NEW_LEVEL=3
echo "$_REMOTE_VER $_NEW_LEVEL $(date +%s)" > "$_SNOOZE_FILE"
```
注意：`{new}` 是 `UPGRADE_AVAILABLE` output 中的 remote version；从 update check result 中替换它。

告诉用户 snooze duration："下次提醒在 24h 后"（或 48h / 1 week，取决于 level）。Tip："在 `~/.gstack/config.yaml` 中设置 `auto_upgrade: true` 可启用 automatic upgrades。"

**如果 "不要再询问"：**
```bash
~/.claude/skills/gstack/bin/gstack-config set update_check false
```
告诉用户："Update checks 已禁用。运行 `~/.claude/skills/gstack/bin/gstack-config set update_check true` 可重新启用。"
继续当前 skill。

### Step 2：检测 install type

```bash
if [ -d "$HOME/.claude/skills/gstack/.git" ]; then
  INSTALL_TYPE="global-git"
  INSTALL_DIR="$HOME/.claude/skills/gstack"
elif [ -d "$HOME/.gstack/repos/gstack/.git" ]; then
  INSTALL_TYPE="global-git"
  INSTALL_DIR="$HOME/.gstack/repos/gstack"
elif [ -d ".claude/skills/gstack/.git" ]; then
  INSTALL_TYPE="local-git"
  INSTALL_DIR=".claude/skills/gstack"
elif [ -d ".agents/skills/gstack/.git" ]; then
  INSTALL_TYPE="local-git"
  INSTALL_DIR=".agents/skills/gstack"
elif [ -d ".claude/skills/gstack" ]; then
  INSTALL_TYPE="vendored"
  INSTALL_DIR=".claude/skills/gstack"
elif [ -d "$HOME/.claude/skills/gstack" ]; then
  INSTALL_TYPE="vendored-global"
  INSTALL_DIR="$HOME/.claude/skills/gstack"
else
  echo "ERROR: gstack not found"
  exit 1
fi
echo "Install type: $INSTALL_TYPE at $INSTALL_DIR"
```

上面打印的 install type 和 directory path 会用于所有后续 steps。

### Step 3：保存旧版本

使用 Step 2 output 中的 install directory：

```bash
OLD_VERSION=$(cat "$INSTALL_DIR/VERSION" 2>/dev/null || echo "unknown")
```

### Step 4：Upgrade

使用 Step 2 检测到的 install type 和 directory：

**对于 git installs**（global-git、local-git）：
```bash
cd "$INSTALL_DIR"
STASH_OUTPUT=$(git stash 2>&1)
git fetch origin
git reset --hard origin/main
./setup
```
如果 `$STASH_OUTPUT` 包含 "Saved working directory"，提醒用户："Note: local changes were stashed. Run `git stash pop` in the skill directory to restore them."

**对于 vendored installs**（vendored、vendored-global）：
```bash
PARENT=$(dirname "$INSTALL_DIR")
TMP_DIR=$(mktemp -d)
git clone --depth 1 https://github.com/garrytan/gstack.git "$TMP_DIR/gstack"
mv "$INSTALL_DIR" "$INSTALL_DIR.bak"
mv "$TMP_DIR/gstack" "$INSTALL_DIR"
cd "$INSTALL_DIR" && ./setup
rm -rf "$INSTALL_DIR.bak" "$TMP_DIR"
```

### Step 4.5：处理 local vendored copy

使用 Step 2 中的 install directory。检查是否还存在 local vendored copy，以及 team mode 是否 active：

```bash
_ROOT=$(git rev-parse --show-toplevel 2>/dev/null)
LOCAL_GSTACK=""
if [ -n "$_ROOT" ] && [ -d "$_ROOT/.claude/skills/gstack" ]; then
  _RESOLVED_LOCAL=$(cd "$_ROOT/.claude/skills/gstack" && pwd -P)
  _RESOLVED_PRIMARY=$(cd "$INSTALL_DIR" && pwd -P)
  if [ "$_RESOLVED_LOCAL" != "$_RESOLVED_PRIMARY" ]; then
    LOCAL_GSTACK="$_ROOT/.claude/skills/gstack"
  fi
fi
_TEAM_MODE=$(~/.claude/skills/gstack/bin/gstack-config get team_mode 2>/dev/null || echo "false")
echo "LOCAL_GSTACK=$LOCAL_GSTACK"
echo "TEAM_MODE=$_TEAM_MODE"
```

**如果 `LOCAL_GSTACK` 非空且 `TEAM_MODE` 为 `true`：**移除 vendored copy。Team mode 使用 global install 作为 single source of truth。

```bash
cd "$_ROOT"
git rm -r --cached .claude/skills/gstack/ 2>/dev/null || true
if ! grep -qF '.claude/skills/gstack/' .gitignore 2>/dev/null; then
  echo '.claude/skills/gstack/' >> .gitignore
fi
rm -rf "$LOCAL_GSTACK"
```
告诉用户："Removed vendored copy at `$LOCAL_GSTACK` (team mode active — global install is the source of truth). Commit the `.gitignore` change when ready."

**如果 `LOCAL_GSTACK` 非空且 `TEAM_MODE` 不是 `true`：**从刚升级过的 primary install 复制更新它（与 README vendored install 同一 approach）：
```bash
mv "$LOCAL_GSTACK" "$LOCAL_GSTACK.bak"
cp -Rf "$INSTALL_DIR" "$LOCAL_GSTACK"
rm -rf "$LOCAL_GSTACK/.git"
cd "$LOCAL_GSTACK" && ./setup
rm -rf "$LOCAL_GSTACK.bak"
```
告诉用户："Also updated vendored copy at `$LOCAL_GSTACK` — commit `.claude/skills/gstack/` when you're ready."

如果 `./setup` 失败，从 backup 恢复并提醒用户：
```bash
rm -rf "$LOCAL_GSTACK"
mv "$LOCAL_GSTACK.bak" "$LOCAL_GSTACK"
```
告诉用户："Sync failed — restored previous version at `$LOCAL_GSTACK`. Run `/gstack-upgrade` manually to retry."

### Step 4.75：运行 version migrations

`./setup` 完成后，运行 old version 与 new version 之间的 migration scripts。Migrations 处理
`./setup` 单独无法覆盖的 state fixes（stale config、orphaned files、directory structure changes）。

```bash
MIGRATIONS_DIR="$INSTALL_DIR/gstack-upgrade/migrations"
if [ -d "$MIGRATIONS_DIR" ]; then
  for migration in $(find "$MIGRATIONS_DIR" -maxdepth 1 -name 'v*.sh' -type f 2>/dev/null | sort -V); do
    # Extract version from filename: v0.15.2.0.sh → 0.15.2.0
    m_ver="$(basename "$migration" .sh | sed 's/^v//')"
    # Run if this migration version is newer than old version
    # (simple string compare works for dotted versions with same segment count)
    if [ "$OLD_VERSION" != "unknown" ] && [ "$(printf '%s\n%s' "$OLD_VERSION" "$m_ver" | sort -V | head -1)" = "$OLD_VERSION" ] && [ "$OLD_VERSION" != "$m_ver" ]; then
      echo "Running migration $m_ver..."
      bash "$migration" || echo "  Warning: migration $m_ver had errors (non-fatal)"
    fi
  done
fi
```

Migrations 是 `gstack-upgrade/migrations/` 中的 idempotent bash scripts。每个都命名为
`v{VERSION}.sh`，且只在从更旧版本升级时运行。如何添加 new migrations 见 CONTRIBUTING.md。

### Step 5：写入 marker + 清理 cache

```bash
mkdir -p ~/.gstack
echo "$OLD_VERSION" > ~/.gstack/just-upgraded-from
rm -f ~/.gstack/last-update-check
rm -f ~/.gstack/update-snoozed
```

### Step 6：展示 What's New

读取 `$INSTALL_DIR/CHANGELOG.md`。找到 old version 和 new version 之间的所有 version entries。
按 theme 总结为 5-7 个 bullets。不要信息过载：聚焦 user-facing changes。除非 internal refactors 很重要，否则跳过。

格式：
```
gstack v{new} — upgraded from v{old}!

What's new:
- [bullet 1]
- [bullet 2]
- ...

Happy shipping!
```

### Step 7：继续

展示 What's New 后，继续用户最初调用的 skill。Upgrade 已完成，不需要进一步 action。

---

## Standalone usage（独立使用）

当直接以 `/gstack-upgrade` 调用时（不是来自 preamble）：

1. 强制 fresh update check（bypass cache）：
```bash
~/.claude/skills/gstack/bin/gstack-update-check --force 2>/dev/null || \
.claude/skills/gstack/bin/gstack-update-check --force 2>/dev/null || true
```
使用 output 判断是否有 upgrade 可用。

2. 如果 `UPGRADE_AVAILABLE <old> <new>`：遵循上面的 Steps 2-6。

3. 如果没有 output（primary 已是最新）：检查 stale local vendored copy。

运行上面的 Step 2 bash block，检测 primary install type 和 directory（`INSTALL_TYPE` 和 `INSTALL_DIR`）。
然后运行上面的 Step 4.5 detection bash block，检查 local vendored copy（`LOCAL_GSTACK`）和 team mode status（`TEAM_MODE`）。

**如果 `LOCAL_GSTACK` 为空**（没有 local vendored copy）：告诉用户 "You're already on the latest version (v{version})."

**如果 `LOCAL_GSTACK` 非空且 `TEAM_MODE` 为 `true`：**使用上面的 Step 4.5 team-mode removal bash block 移除 vendored copy。告诉用户："Global v{version} is up to date. Removed stale vendored copy (team mode active). Commit the `.gitignore` change when ready."

**如果 `LOCAL_GSTACK` 非空且 `TEAM_MODE` 不是 `true`**，比较 versions：
```bash
PRIMARY_VER=$(cat "$INSTALL_DIR/VERSION" 2>/dev/null || echo "unknown")
LOCAL_VER=$(cat "$LOCAL_GSTACK/VERSION" 2>/dev/null || echo "unknown")
echo "PRIMARY=$PRIMARY_VER LOCAL=$LOCAL_VER"
```

**如果 versions 不同：**遵循上面的 Step 4.5 sync bash block，从 primary 更新 local copy。告诉用户："Global v{PRIMARY_VER} is up to date. Updated local vendored copy from v{LOCAL_VER} -> v{PRIMARY_VER}. Commit `.claude/skills/gstack/` when you're ready."

**如果 versions 相同：**告诉用户 "You're on the latest version (v{PRIMARY_VER}). Global and local vendored copy are both up to date."
