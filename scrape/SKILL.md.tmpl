---
name: scrape
version: 1.0.0
description: |
  从 web page 拉取数据。对新 intent 的首次调用会通过 $B primitives prototype flow，
  并返回 JSON。后续匹配同一 intent 的调用会 route 到 codified browser-skill，
  约 200ms 返回。Read-only：mutating flows（form fills、clicks、submissions）
  请使用 /automate。当用户要求 "scrape"、"get data from"、"pull"、
  "extract from" 或 "what's on" 某 page 时使用。（gstack）
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
triggers:
  - scrape this page
  - get data from
  - pull from
  - extract from
  - what is on
---

{{PREAMBLE}}

# /scrape — 从 page 拉取数据

从 web 获取数据的单一 entry point。底层有两条 paths：

1. **Match path**（约 200ms）：如果用户 intent 匹配 existing browser-skill 的 triggers，
   通过 `$B skill run <name>` 运行它，并 emit JSON。
2. **Prototype path**（约 30s）：还没有 matching skill，所以用 `$B` primitives 驱动 page，
   返回 JSON，并建议 `/skillify`，让下一次调用进入 match path。

按 contract，它是 read-only。如果 intent 暗示 writing（submitting forms、clicking buttons
that mutate state），拒绝并 route 到 `/automate`。

## Step 1 — 确定 intent

`/scrape` 后面的用户请求就是 intent。如果没有包含 intent，只问一次：

> "What do you want to scrape? Describe it in one line, e.g. 'top stories
> on Hacker News' or 'product names + prices on example.com/products'."

不要一开始就问多个 clarifying questions。任何进一步问题都放到 prototype path 中，
那里成本更低。

## Step 2 — 拒绝 mutating intents

如果 intent 暗示 writes：例如 *submit*、*post*、*send*、*log in*、*click X*、
*fill the form*、*delete*、*create*、*order*、*book* 这类 verbs，则回应：

> "/scrape is read-only. For mutating flows, use /automate (browser-skills
> Phase 2 P0 in TODOS.md — not yet shipped). Until then, use $B click /
> $B fill / $B type directly."

停止。不要进入 match 或 prototype path。

## Step 3 — Match phase

列出 existing browser-skills：

```bash
$B skill list
```

对每个 skill，`$B skill show <name>` 会展示完整 SKILL.md，包括 `triggers:`、
`description:` 和 `host:`。阅读它们，判断用户 intent 是否 semantically matches 其中一个。

Confident match 意味着**以下三项都为真**：

- Intent 的 domain 匹配 skill 的 `host`（或其 hostnames 之一）
- 某个 `triggers:` phrase 或 `description:` 覆盖 intent 请求的同类 data
- Intent 不需要 skill 在 `args:` 中未声明的 args

如果 matched，从 intent 解析任何 `--arg key=value`（zero-arg skills 则不传），然后运行：

```bash
$B skill run <name> [--arg key=value ...]
```

Emit 该 skill 打印到 stdout 的 JSON。停止。

如果 matching ambiguous（两个 skills 都看似适合），选择 narrower-tier one
（project > global > bundled；`$B skill list` 会显示 tier）。如果仍 ambiguous，
进入 prototype path，不要猜错。

## Step 4 — Prototype phase（原型阶段）

无 match。使用 `$B` primitives 驱动 page：

1. `$B goto <url>`：navigate 到 target。用户 intent 通常会指出 host 或 URL；直接使用。
2. `$B snapshot --text`（或 `$B text`）：获取 page 的 clean text view，用来找 selectors。
3. `$B html`：需要 parse structured data（lists、tables、repeated rows）时拉取 raw HTML。
4. `$B links`：当 intent 是收集 URLs 时使用。
5. Iterate：尝试 selector、检查 output、refine。

将 result 作为 JSON emit 到 stdout（一个 document，不 pretty-print）。
使用 stable shape：通常是 `{ "items": [...], "count": N }` 或类似结构，
让 downstream consumers 可以把它当 data 处理。

## Step 5 — Skillify nudge（提示转为 skill）

Successful prototype 后，精确追加一行：

> "Say /skillify to make this a permanent skill (200ms on next call)."

这就是完整 nudge。不要 nag，不要列 pros，不要 push。
Proactive surfacing 是 Phase 3 knob（`gstack-config browser_skillify_prompts`），
不是此 skill 的职责。

## 当 prototype 失败

如果 page 加载成功，但 data extraction 在 3-4 次 selector attempts 后仍没有产出合理 JSON shape：

- 报告你尝试了什么、返回了什么、阻塞点是什么（lazy-loaded、JS-rendered、paywalled 等）。
- 不要写 partial result 并称之为 done。
- 不要在 broken prototype 上建议 /skillify。
- 询问用户是否想要 (a) 尝试不同 selector，(b) 切换到不同 page，或 (c) 停止。

## 此 skill 不做什么

- Mutating actions（/automate shipped 后使用它，或直接用 $B primitives）
- Auth flows / cookie import（先用 /setup-browser-cookies）
- Multi-page crawls（每次调用都是 one-shot）
- 任何要求 daemon 不运行的事

## Output discipline（输出纪律）

Match path 返回 matched skill emit 的 JSON。Prototype path 返回你构造的 JSON。两种情况下：

- 一个 JSON document，输出到 stdout。
- Stderr（或 chat）用于 logs 和 skillify nudge。
- 除非用户要求 explanation，不要在 chat reply 中围绕 JSON 嵌入 prose：许多 `/scrape`
  callers 会把 output pipe 给 `jq`。

{{LEARNINGS_LOG}}
