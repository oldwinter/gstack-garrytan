---
name: investigate
preamble-tier: 2
version: 1.0.0
description: |
  带 root cause investigation 的系统化 debugging。四个阶段：investigate、
  analyze、hypothesize、implement。Iron Law：没有 root cause 就不修。
  当用户要求 "debug this"、"fix this bug"、"why is this broken"、
  "investigate this error" 或 "root cause analysis" 时使用。
  当用户报告 errors、500 errors、stack traces、unexpected behavior、
  "it was working yesterday"，或正在排查某物为何停止工作时，主动调用此 skill
  （不要直接 debug）。（gstack）
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Grep
  - Glob
  - AskUserQuestion
  - WebSearch
triggers:
  - debug this
  - fix this bug
  - why is this broken
  - root cause analysis
  - investigate this error
hooks:
  PreToolUse:
    - matcher: "Edit"
      hooks:
        - type: command
          command: 'bash -c ''S="${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh"; [ -x "$S" ] || S="${CLAUDE_SKILL_DIR}/../gstack-freeze/bin/check-freeze.sh"; [ -x "$S" ] && bash "$S" || exit 0'''
          statusMessage: "Checking debug scope boundary..."
    - matcher: "Write"
      hooks:
        - type: command
          command: 'bash -c ''S="${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh"; [ -x "$S" ] || S="${CLAUDE_SKILL_DIR}/../gstack-freeze/bin/check-freeze.sh"; [ -x "$S" ] && bash "$S" || exit 0'''
          statusMessage: "Checking debug scope boundary..."
gbrain:
  schema: 1
  context_queries:
    - id: prior-investigations
      kind: list
      filter:
        type: timeline
        tags_contains: "repo:{repo_slug}"
        content_contains: "investigate"
      sort: updated_at_desc
      limit: 5
      render_as: "## Prior investigations in this repo"
    - id: project-learnings
      kind: filesystem
      glob: "~/.gstack/projects/{repo_slug}/learnings.jsonl"
      tail: 10
      render_as: "## Recent learnings (patterns + pitfalls)"
    - id: recent-eureka
      kind: filesystem
      glob: "~/.gstack/analytics/eureka.jsonl"
      tail: 5
      render_as: "## Recent eureka moments (cross-project)"
---

{{PREAMBLE}}

# Systematic Debugging（系统化调试）

## Iron Law（铁律）

**先做 ROOT CAUSE INVESTIGATION；没有 ROOT CAUSE 就不修。**

修 symptoms 会制造反复出现的 debugging 问题。每个没有解决 root cause 的 fix，都会让下一个 bug 更难定位。
先找到 root cause，再修。

---

{{GBRAIN_CONTEXT_LOAD}}

## Phase 1：Root Cause Investigation

形成任何 hypothesis 前，先收集 context。

1. **收集 symptoms：**阅读 error messages、stack traces 和 reproduction steps。如果用户没有提供足够 context，每次只通过 AskUserQuestion 问一个问题。

2. **读代码：**从 symptom 沿 code path 回溯到潜在原因。用 Grep 找所有 references，用 Read 理解逻辑。

3. **检查最近变更：**
   ```bash
   git log --oneline -20 -- <affected-files>
   ```
   之前是否工作正常？发生了什么变化？Regression 意味着 root cause 在 diff 里。

4. **Reproduce：**你能确定性触发 bug 吗？如果不能，继续收集更多 evidence 后再推进。

5. **检查 investigation history：**在 prior learnings 中搜索同一 files 上的 investigations。同一区域反复出现 bugs 是 architecture smell。如果有 prior investigations，记录 patterns，并检查 root cause 是否是 structural。

{{LEARNINGS_SEARCH:query=debug investigation root cause hypothesis bug fix}}

输出：**"Root cause hypothesis: ..."**：一个关于哪里错了、为什么错的具体且可测试 claim。

### 针对刚命名的 hypothesis 刷新 learnings

上面的 skill 顶部 learnings pull 使用的是宽泛的 "debug investigation"。现在你已经有了具体 hypothesis，
请用该 hypothesis 重新拉取 learnings，让同类问题形状的 prior fixes 浮现出来。

从 hypothesis 中选一个 keyword。Keyword 应该是名词：失败 component name、你怀疑的 file basename（不带 extension），
或 bug noun。Keyword 必须只包含 alphanumeric 或 hyphen：不要 quotes、slashes、dots、colons 或 whitespace。
如果候选词包含这些字符，简化为 alphanumeric stem。

示例（investigate-specific）：好的 keywords 是 `auth-cookie`、`session-expiry`、`redirect-loop`。
不好的例子：`auth.ts:47`、`fix the auth bug`、`<hypothesis-keyword>`。

```bash
~/.claude/skills/gstack/bin/gstack-learnings-search --query "<your-keyword>" --limit 5 2>/dev/null || true
```

如果返回了任何 learnings，用一句话说明哪一条适用于本次 investigation。如果没有返回，继续即可：
没有匹配的 prior learning 本身也是有用信息。

---

## Scope Lock（范围锁定）

形成 root cause hypothesis 后，将 edits 锁定到受影响 module，防止 scope creep。

```bash
_FREEZE_SCRIPT="${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh"
[ -x "$_FREEZE_SCRIPT" ] || _FREEZE_SCRIPT="${CLAUDE_SKILL_DIR}/../gstack-freeze/bin/check-freeze.sh"
[ -x "$_FREEZE_SCRIPT" ] && echo "FREEZE_AVAILABLE" || echo "FREEZE_UNAVAILABLE"
```

**如果 FREEZE_AVAILABLE：**识别包含 affected files 的最窄目录。将其写入 freeze state file：

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-paths)"
STATE_DIR="$GSTACK_STATE_ROOT"
mkdir -p "$STATE_DIR"
echo "<detected-directory>/" > "$STATE_DIR/freeze-dir.txt"
echo "Debug scope locked to: <detected-directory>/"
```

将 `<detected-directory>` 替换为实际 directory path（例如 `src/auth/`）。告诉用户：
"Edits restricted to `<dir>/` for this debug session. This prevents changes to unrelated code. Run `/unfreeze` to remove the restriction."

如果 bug 横跨整个 repo，或 scope 确实不清楚，跳过 lock 并说明原因。

**如果 FREEZE_UNAVAILABLE：**跳过 scope lock。Edits 不受限。

---

## Phase 2：Pattern Analysis

检查此 bug 是否匹配已知 pattern：

| Pattern | Signature | Where to look |
|---------|-----------|---------------|
| Race condition | Intermittent, timing-dependent | Concurrent access to shared state |
| Nil/null propagation | NoMethodError, TypeError | Missing guards on optional values |
| State corruption | Inconsistent data, partial updates | Transactions, callbacks, hooks |
| Integration failure | Timeout, unexpected response | External API calls, service boundaries |
| Configuration drift | Works locally, fails in staging/prod | Env vars, feature flags, DB state |
| Stale cache | Shows old data, fixes on cache clear | Redis, CDN, browser cache, Turbo |

也检查：
- `TODOS.md` 中相关 known issues
- `git log` 中同一区域的 prior fixes：**同一 files 中反复出现 bugs 是 architectural smell**，不是巧合

**External pattern search：**如果 bug 不匹配上述 known pattern，使用 WebSearch 搜索：
- "{framework} {generic error type}"：**先 sanitize：**移除 hostnames、IPs、file paths、SQL、customer data。搜索 error category，不搜 raw message。
- "{library} {component} known issues"

如果 WebSearch 不可用，跳过此 search 并继续 hypothesis testing。如果发现 documented solution 或 known dependency bug，
在 Phase 3 将其作为 candidate hypothesis 呈现。

---

## Phase 3：Hypothesis Testing

写任何 fix 之前，先验证 hypothesis。

1. **确认 hypothesis：**在疑似 root cause 处添加临时 log statement、assertion 或 debug output。运行 reproduction。Evidence 是否匹配？

2. **如果 hypothesis 错了：**形成下一个 hypothesis 前，考虑搜索 error。**先 sanitize**：
   从 error message 中移除 hostnames、IPs、file paths、SQL fragments、customer identifiers
   和任何 internal/proprietary data。只搜索 generic error type 和 framework context：
   "{component} {sanitized error type} {framework version}"。如果 error message 过于具体，无法安全 sanitize，
   跳过 search。如果 WebSearch 不可用，也跳过并继续。然后回到 Phase 1，收集更多 evidence。不要猜。

3. **3-strike rule：**如果 3 个 hypotheses 都失败，**STOP**。使用 AskUserQuestion：
   ```
   3 hypotheses tested, none match. This may be an architectural issue
   rather than a simple bug.

   A) Continue investigating — I have a new hypothesis: [describe]
   B) Escalate for human review — this needs someone who knows the system
   C) Add logging and wait — instrument the area and catch it next time
   ```

**Red flags**：如果看到以下情况，放慢：
- "Quick fix for now"：没有 "for now"。要么修对，要么 escalate。
- 在 tracing data flow 前提出 fix：你是在猜。
- 每个 fix 都暴露出别处的新问题：错的是 layer，不是某行 code。

---

## Phase 4：Implementation

一旦确认 root cause：

1. **修 root cause，而不是 symptom。**使用能消除实际问题的最小变更。

2. **Minimal diff：**触碰最少 files，改动最少 lines。克制 refactor adjacent code 的冲动。

3. **编写 regression test**，它应当：
   - 没有 fix 时 **fails**（证明 test 有意义）
   - 有 fix 时 **passes**（证明 fix 有效）

4. **运行 full test suite。**粘贴 output。不允许 regressions。

5. **如果 fix 触碰超过 5 个 files：**使用 AskUserQuestion 标记 blast radius：
   ```
   This fix touches N files. That's a large blast radius for a bug fix.
   A) Proceed — the root cause genuinely spans these files
   B) Split — fix the critical path now, defer the rest
   C) Rethink — maybe there's a more targeted approach
   ```

---

## Phase 5：Verification & Report

**Fresh verification：**重新 reproduce 原始 bug scenario，并确认已修复。这不是可选项。

运行 test suite 并粘贴 output。

输出结构化 debug report：
```
DEBUG REPORT
════════════════════════════════════════
Symptom:         [what the user observed]
Root cause:      [what was actually wrong]
Fix:             [what was changed, with file:line references]
Evidence:        [test output, reproduction attempt showing fix works]
Regression test: [file:line of the new test]
Related:         [TODOS.md items, prior bugs in same area, architectural notes]
Status:          DONE | DONE_WITH_CONCERNS | BLOCKED
════════════════════════════════════════
```

将 investigation 记录为 future sessions 的 learning。使用 `type: "investigation"`，
并包含 affected files，让未来同一区域的 investigations 可以找到它：

```bash
~/.claude/skills/gstack/bin/gstack-learnings-log '{"skill":"investigate","type":"investigation","key":"ROOT_CAUSE_KEY","insight":"ROOT_CAUSE_SUMMARY","confidence":9,"source":"observed","files":["affected/file1.ts","affected/file2.ts"]}'
```

{{LEARNINGS_LOG}}

{{GBRAIN_SAVE_RESULTS}}

---

## Important Rules（重要规则）

- **3+ 次 fix attempts 失败 -> STOP 并质疑 architecture。**问题是 wrong architecture，不是 failed hypothesis。
- **绝不应用无法验证的 fix。**如果不能 reproduce 和 confirm，就不要 ship。
- **绝不说 "this should fix it."**验证并证明它。运行 tests。
- **如果 fix 触碰超过 5 个 files -> 在继续前用 AskUserQuestion**询问 blast radius。
- **Completion status：**
  - DONE：找到 root cause、已应用 fix、已写 regression test、所有 tests pass
  - DONE_WITH_CONCERNS：已修复但无法完全验证（例如 intermittent bug、需要 staging）
  - BLOCKED：investigation 后 root cause 仍不清楚，已 escalated
