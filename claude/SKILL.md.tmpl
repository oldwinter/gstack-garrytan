---
name: claude
preamble-tier: 3
version: 1.0.0
description: |
  面向 non-Claude hosts 的 Claude Code CLI wrapper - 三种模式。Review：通过
  claude -p 做 independent diff review。Challenge：adversarial failure-mode
  review。Consult：用 read-only file tools 询问 Claude 关于 repo 的问题。
  当用户要求 "claude review"、"claude challenge"、"ask claude"、
  "second opinion from claude" 或 "outside voice" 时使用。(gstack)
triggers:
  - claude review
  - claude challenge
  - ask claude
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

# /claude - Claude Outside Voice（Claude 外部视角）

你正在从 non-Claude host 运行 `/claude` skill。它封装 `claude -p`，
用于获取独立的 Claude Code second opinion，同时不允许 nested Claude 修改文件。

生成的 external invocation name 是 `gstack-claude`。

---

## Step 0: 检查 Claude CLI

```bash
CLAUDE_BIN=$(command -v claude 2>/dev/null || echo "")
[ -z "$CLAUDE_BIN" ] && echo "NOT_FOUND" || echo "FOUND: $CLAUDE_BIN"
```

如果是 `NOT_FOUND`，停止并告诉用户：
"未找到 Claude CLI。请安装 Claude Code，然后重新运行此 skill。"

检查 auth：

```bash
if [ -f "$HOME/.claude/.credentials.json" ] || [ -n "${ANTHROPIC_API_KEY:-}" ]; then
  echo "AUTH_FOUND"
else
  echo "AUTH_MISSING"
fi
```

如果是 `AUTH_MISSING`，停止并告诉用户：
"未找到 Claude authentication。请交互式运行 `claude` 登录，或导出 `ANTHROPIC_API_KEY`，然后重新运行此 skill。"

---

## Safety Boundary（安全边界）

Nested Claude 必须专注于用户的 repository，并且不得从此 skill 内部运行 gstack
skills。

所有 `claude -p` 调用 MUST include：

- `--disable-slash-commands`
- Review/challenge：`--tools ""`
- Consult：`--allowedTools Read,Grep,Glob --disallowedTools Bash,Edit,Write`

在这个 skill 中，永远不要把 `Bash`、`Edit` 或 `Write` 传给 nested Claude。

所有 prompts MUST 写入 temp file，并通过 stdin 输入。永远不要把 user text 直接插入 shell command。

---

## Step 1: 检测模式

解析用户输入：

1. `/claude review` 或 `/claude review <instructions>` - **Review mode**（Step 2A）
2. `/claude challenge` 或 `/claude challenge <focus>` - **Challenge mode**（Step 2B）
3. 无 arguments 的 `/claude`，或 `/claude <anything else>` - **Consult mode**（Step 2C）

如果 mode 不明显且存在 diff，询问用户要 review、challenge 还是 consult。

---

## Shared Helpers（共享辅助片段）

每种 mode 都使用这些 shell snippets。

创建 temp files：

```bash
PROMPT_FILE=$(mktemp /tmp/gstack-claude-prompt-XXXXXX)
RESP_FILE=$(mktemp /tmp/gstack-claude-response-XXXXXX.json)
ERR_FILE=$(mktemp /tmp/gstack-claude-error-XXXXXX.txt)
```

每种 mode 结束时清理：

```bash
rm -f "$PROMPT_FILE" "$RESP_FILE" "$ERR_FILE"
```

解析 JSON output：

```bash
python3 - "$RESP_FILE" <<'PY'
import json, sys
path = sys.argv[1]
try:
    obj = json.load(open(path))
except Exception as exc:
    print(f"CLAUDE_JSON_PARSE_ERROR: {exc}")
    sys.exit(0)

if obj.get("is_error"):
    print("CLAUDE_ERROR: true")

result = obj.get("result") or obj.get("response") or ""
if result:
    print(result)

usage = obj.get("usage") or {}
input_tokens = usage.get("input_tokens", 0) or 0
output_tokens = usage.get("output_tokens", 0) or 0
cache_read = usage.get("cache_read_input_tokens", 0) or 0
model = obj.get("model") or "unknown"
session_id = obj.get("session_id") or ""

print(f"\nTokens: input={input_tokens} output={output_tokens} cache_read={cache_read} | Model: {model}")
if session_id:
    print(f"SESSION_ID:{session_id}")
PY
```

如果 stderr 包含 `auth`、`login` 或 `unauthorized`，告诉用户：
"Claude authentication failed. Run `claude` interactively to authenticate or export `ANTHROPIC_API_KEY`."

---

## Step 2A：Review Mode（审查模式）

用 nested Claude 以 tool-less mode review 当前 branch diff。

1. Fetch base 并捕获 diff：

```bash
_REPO_ROOT=$(git rev-parse --show-toplevel) || { echo "ERROR: not in a git repo" >&2; exit 1; }
cd "$_REPO_ROOT"
DIFF_FILE=$(mktemp /tmp/gstack-claude-diff-XXXXXX.patch)
git fetch origin <base> --quiet 2>/dev/null || true
git diff "origin/<base>" > "$DIFF_FILE" 2>/dev/null || git diff "<base>" > "$DIFF_FILE"
```

如果 diff file 为空，停止并说明：
"无需 review：相对 base branch 没有 changes。"

2. 写入 prompt file：

```bash
cat > "$PROMPT_FILE" <<'EOF'
你是一个 brutally honest Claude Code reviewer。Review 这个 git diff，寻找 bugs、
production failure modes、security issues、missing tests 和 maintainability
problems。直接，不要 compliments。尽可能引用 files 和 changed code。

Additional user instructions, if any:
<custom review instructions>

DIFF:
EOF
cat "$DIFF_FILE" >> "$PROMPT_FILE"
```

3. 运行 Claude：

```bash
cat "$PROMPT_FILE" | claude -p --output-format json --disable-slash-commands --tools "" > "$RESP_FILE" 2>"$ERR_FILE"
```

4. 展示解析后的 output：

```
CLAUDE SAYS (code review):
============================================================
<parsed result from RESP_FILE>
============================================================
```

5. 清理：

```bash
rm -f "$DIFF_FILE" "$PROMPT_FILE" "$RESP_FILE" "$ERR_FILE"
```

---

## Step 2B：Challenge Mode（挑战模式）

用 nested Claude 以 tool-less mode 运行 adversarial failure-mode review。

1. 使用 Review mode 中相同的 diff commands 捕获 diff。

2. 写入 prompt：

```bash
cat > "$PROMPT_FILE" <<'EOF'
你是一个 adversarial Claude Code reviewer。请在 users 遇到问题前尝试打破这个 change。
找出 edge cases、race conditions、security holes、resource leaks、silent data
corruption、bad error handling 和 operational failure modes。要 thorough，不要
compliments。如果用户提供了 focus area，优先处理它。

Focus area, if any:
<focus>

DIFF:
EOF
cat "$DIFF_FILE" >> "$PROMPT_FILE"
```

3. 运行 Claude：

```bash
cat "$PROMPT_FILE" | claude -p --output-format json --disable-slash-commands --tools "" > "$RESP_FILE" 2>"$ERR_FILE"
```

4. 展示解析后的 output：

```
CLAUDE SAYS (adversarial challenge):
============================================================
<parsed result from RESP_FILE>
============================================================
```

5. 清理：

```bash
rm -f "$DIFF_FILE" "$PROMPT_FILE" "$RESP_FILE" "$ERR_FILE"
```

---

## Step 2C：Consult Mode（咨询模式）

询问 Claude 关于 repository 的问题。Consult mode 可以 inspect files，但只能使用
read-only tools。

1. 检查 existing Claude session：

```bash
cat .context/claude-session-id 2>/dev/null || echo "NO_SESSION"
```

如果 session 存在，询问用户是继续它还是 start fresh。

2. 写入 prompt：

```bash
cat > "$PROMPT_FILE" <<'EOF'
你是 Claude Code，作为这个 repository 的独立外部声音。
直接回答用户的问题。你只能用 Read、Grep 和 Glob 检查 repository files。
不要使用 Bash。不要编辑或写入 files。不要调用 slash commands 或 gstack skills。

USER QUESTION:
<user prompt>
EOF
```

3. 运行 Claude。

对于 new session：

```bash
cat "$PROMPT_FILE" | claude -p --output-format json --disable-slash-commands --allowedTools Read,Grep,Glob --disallowedTools Bash,Edit,Write > "$RESP_FILE" 2>"$ERR_FILE"
```

对于 resumed session：

```bash
cat "$PROMPT_FILE" | claude -p --resume "<session-id>" --output-format json --disable-slash-commands --allowedTools Read,Grep,Glob --disallowedTools Bash,Edit,Write > "$RESP_FILE" 2>"$ERR_FILE"
```

4. 解析并保存 session id：

```bash
SESSION_ID=$(python3 - "$RESP_FILE" <<'PY'
import json, sys
try:
    obj = json.load(open(sys.argv[1]))
    print(obj.get("session_id") or "")
except Exception:
    print("")
PY
)
if [ -n "$SESSION_ID" ]; then
  mkdir -p .context
  printf "%s\n" "$SESSION_ID" > .context/claude-session-id
fi
```

5. 展示解析后的 output：

```
CLAUDE SAYS (consult):
============================================================
<parsed result from RESP_FILE>
============================================================
Session saved - run /claude again to continue this conversation.
```

6. 清理：

```bash
rm -f "$PROMPT_FILE" "$RESP_FILE" "$ERR_FILE"
```

---

## Error Handling（错误处理）

- **Binary not found:** 停止并给出 install instructions。
- **Auth missing:** 停止并给出 login/API key instructions。
- **Auth failure from stderr:** 展示 stderr line，并要求用户 re-authenticate。
- **JSON parse failure:** 展示 `$RESP_FILE` 的 raw stdout 和 `$ERR_FILE` 的 stderr。
- **Empty response:** 告诉用户 "Claude 没有返回 response。请检查 stderr 中的 errors。"
- **Resume failure:** 删除 `.context/claude-session-id`，并用 fresh session 重试。

---

## Important Rules（重要规则）

- Nested Claude 在 consult mode 下是 read-only，在 review/challenge 下是 tool-less。
- 始终包含 `--disable-slash-commands`。
- 永远不要把 `Bash`、`Edit` 或 `Write` 传给 nested Claude。
- 永远不要把 user text 插入 shell command。
- 忠实呈现 Claude 的 response，然后再补充任何 host-agent synthesis。
